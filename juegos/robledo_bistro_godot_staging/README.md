# Robledo Bistro Godot full project staging payload

This temporary payload is expanded automatically by GitHub Actions. On `main`, the workflow commits the expanded `juegos/robledo_bistro_godot/` project and removes this staging directory. The payload contains the Godot source and original SVG assets; the workflow copies the repository's official school logo into the local game project before export.
