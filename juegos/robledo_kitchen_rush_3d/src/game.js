import * as THREE from 'three';
import { RoundedBoxGeometry } from 'three/examples/jsm/geometries/RoundedBoxGeometry.js';

const COLORS = {
  ink: 0x223038, cream: 0xfff4dc, floorA: 0xe5d7bd, floorB: 0xd8c8ad,
  red: 0xe34f51, blue: 0x4387db, yellow: 0xf2bd3f, green: 0x4d9a67,
  orange: 0xef7c38, wood: 0x9b6946, steel: 0xaab6b8, darkSteel: 0x66767a,
  wall: 0xf2e8d7, counter: 0xf7f0e5, trim: 0x41545b, water: 0x4fb7dc,
  kitchenZone: 0x79b9bd, diningZone: 0xd9aa61,
};

const ROLE_META = {
  chef: {
    id: 'chef', name: 'Head Chef', icon: '🔥',
    description: 'Cooks, fries and bakes 35% faster. Burn windows are more forgiving.',
    cook: 1.35, prep: 1.0, wash: 1.0, move: 1.0, tip: 1.0, botBias: 'hot',
  },
  prep: {
    id: 'prep', name: 'Prep Specialist', icon: '🔪',
    description: 'Chops 55% faster and moves slightly faster while carrying ingredients.',
    cook: 1.0, prep: 1.55, wash: 1.0, move: 1.06, tip: 1.0, botBias: 'prep',
  },
  service: {
    id: 'service', name: 'Service Captain', icon: '🍽',
    description: 'Moves 15% faster, washes 40% faster and earns 15% more service tips.',
    cook: 1.0, prep: 1.0, wash: 1.4, move: 1.15, tip: 1.15, botBias: 'service',
  },
};

const RECIPES = {
  burger: { id:'burger', name:'Rush Burger', icon:'🍔', components:['bun:raw','meat:cooked','tomato:chopped'], color:'#e46f3d', price:180, steps:['Take bun, meat and tomato','Chop tomato','Cook meat on stove','Assemble on a clean plate'] },
  salad: { id:'salad', name:'Garden Salad', icon:'🥗', components:['lettuce:chopped','tomato:chopped'], color:'#56a65f', price:140, steps:['Take lettuce and tomato','Chop both ingredients','Assemble on a clean plate'] },
  fries: { id:'fries', name:'Golden Fries', icon:'🍟', components:['potato:fried'], color:'#e8b23f', price:150, steps:['Take potato','Chop potato','Fry until golden','Plate before it burns'] },
  pizza: { id:'pizza', name:'Mini Pizza', icon:'🍕', components:['dough:raw','tomato:chopped','cheese:raw'], baked:true, color:'#d95945', price:210, steps:['Take dough, tomato and cheese','Chop tomato','Assemble on a clean plate','Bake until ready'] },
  grill: { id:'grill', name:'Grill Plate', icon:'🍗', components:['meat:cooked','lettuce:chopped'], color:'#8b563e', price:200, steps:['Take meat and lettuce','Cook meat','Chop lettuce','Assemble on a clean plate'] },
  toast: { id:'toast', name:'Cheesy Toast', icon:'🥪', components:['bun:raw','cheese:raw'], baked:true, color:'#e2a037', price:165, steps:['Take bun and cheese','Assemble on a clean plate','Bake until golden'] },
};

const INGREDIENT_META = {
  tomato:{name:'Tomato',emoji:'🍅',choppable:true}, lettuce:{name:'Lettuce',emoji:'🥬',choppable:true},
  meat:{name:'Meat',emoji:'🥩',cook:'stove'}, potato:{name:'Potato',emoji:'🥔',choppable:true,cook:'fryer'},
  dough:{name:'Dough',emoji:'🫓'}, cheese:{name:'Cheese',emoji:'🧀'}, bun:{name:'Bun',emoji:'🥯'},
};

const LEVELS = [
  {id:0,name:'Bistro Basics',duration:210,spawnEvery:18,patience:76,thresholds:[950,1700,2600],tint:0xb9d8c5,budget:860},
  {id:1,name:'Split Service',duration:240,spawnEvery:14,patience:66,thresholds:[1300,2250,3400],tint:0xc9d6e8,budget:900},
  {id:2,name:'Dinner Rush',duration:270,spawnEvery:11,patience:58,thresholds:[1800,3100,4550],tint:0xe5c9bd,budget:950},
];

const CUSTOMER_TYPES = [
  {id:'student',label:'STUDENTS',patience:1.12,tip:.9,color:0x4d70ae},
  {id:'family',label:'FAMILY',patience:1.25,tip:1.0,color:0x7a4f9e},
  {id:'quick',label:'QUICK LUNCH',patience:.82,tip:1.15,color:0xc6604b},
  {id:'foodie',label:'FOODIE',patience:1.0,tip:1.35,color:0x3d8b75},
];

const FIXTURES = {
  table:{id:'table',label:'Dining Table',icon:'🍽',cost:48,zone:'dining',w:2.5,d:2.5},
  counter:{id:'counter',label:'Counter',icon:'▰',cost:22,zone:'kitchen',w:1.45,d:1.2},
  prep:{id:'prep',label:'Prep Board',icon:'🔪',cost:38,zone:'kitchen',w:1.45,d:1.2},
  stove:{id:'stove',label:'Stove',icon:'🔥',cost:48,zone:'kitchen',w:1.45,d:1.2},
  fryer:{id:'fryer',label:'Fryer',icon:'🍟',cost:52,zone:'kitchen',w:1.45,d:1.2},
  oven:{id:'oven',label:'Oven',icon:'♨️',cost:62,zone:'kitchen',w:1.45,d:1.2},
  sink:{id:'sink',label:'Sink',icon:'🚿',cost:34,zone:'kitchen',w:1.45,d:1.2},
  plate:{id:'plate',label:'Plate Rack',icon:'🥣',cost:24,zone:'kitchen',w:1.45,d:1.2},
  trash:{id:'trash',label:'Trash',icon:'🗑',cost:16,zone:'kitchen',w:1.45,d:1.2},
  grocery:{id:'grocery',label:'Grocery Market + Freezer',icon:'🧊',cost:78,zone:'kitchen',w:5.4,d:1.55,storage:true},
};

const LEGACY_GROCERY_KEYS = new Set(['tomato','lettuce','meat','potato','bun','cheese','dough']);

const BUSINESS_DEFAULT = {
  cash: 430,
  satisfaction: 68,
  expansionTier: 0,
  plan: [],
  totalServed: 0,
  fastServes: 0,
  totalRevenue: 0,
};

const EXPANSION_TIERS = [
  {
    id:0,name:'Starter Bistro',upgradeCost:320,requiredSatisfaction:72,
    xMax:7.2,zMin:-6.2,zMax:6.2,tableCap:2,customerCap:2,partySize:2,menuSlots:2,minTables:2,
    fixtureCaps:{table:2,counter:1,prep:1,stove:1,fryer:0,oven:0,sink:1,plate:1,trash:1,grocery:1},
    recipes:['salad','burger'],
  },
  {
    id:1,name:'Growing Café',upgradeCost:620,requiredSatisfaction:78,
    xMax:9.6,zMin:-7.6,zMax:7.6,tableCap:4,customerCap:3,partySize:3,menuSlots:3,minTables:2,
    fixtureCaps:{table:4,counter:2,prep:2,stove:1,fryer:1,oven:1,sink:1,plate:1,trash:1,grocery:1},
    recipes:['salad','burger','fries','toast'],
  },
  {
    id:2,name:'Neighborhood Restaurant',upgradeCost:980,requiredSatisfaction:84,
    xMax:12.0,zMin:-9.2,zMax:9.2,tableCap:6,customerCap:4,partySize:3,menuSlots:4,minTables:3,
    fixtureCaps:{table:6,counter:3,prep:2,stove:2,fryer:1,oven:1,sink:2,plate:2,trash:1,grocery:1},
    recipes:['salad','burger','fries','toast','grill'],
  },
  {
    id:3,name:'Robledo Grand Bistro',upgradeCost:0,requiredSatisfaction:0,
    xMax:14.2,zMin:-10.9,zMax:10.9,tableCap:9,customerCap:5,partySize:4,menuSlots:5,minTables:3,
    fixtureCaps:{table:9,counter:4,prep:3,stove:2,fryer:2,oven:2,sink:2,plate:2,trash:2,grocery:1},
    recipes:['salad','burger','fries','toast','grill','pizza'],
  },
];

const FAST_SERVICE_SECONDS = 30;
const MAX_LIVES = 3;

const GEOMETRY_QUESTIONS = [
  {q:'A rectangle measures 8 m by 5 m. What is its area?',a:['13 m²','26 m²','40 m²','80 m²'],c:2},
  {q:'A square has side length 6 cm. What is its perimeter?',a:['12 cm','24 cm','36 cm','18 cm'],c:1},
  {q:'A triangle has base 10 cm and height 7 cm. What is its area?',a:['17 cm²','35 cm²','70 cm²','140 cm²'],c:1},
  {q:'A circle has radius 4 cm. Which expression gives its area?',a:['4π','8π','16π','32π'],c:2},
  {q:'A trapezoid has bases 6 cm and 10 cm and height 4 cm. Its area is…',a:['16 cm²','24 cm²','32 cm²','64 cm²'],c:2},
];

const TUTORIAL = [
  {title:'Start small, grow smart',visual:'🏠 → 🏪 → 🍽',body:'Your first restaurant is intentionally compact: only two tables, two active menu recipes and a small customer limit. Strong service earns cash and satisfaction, which unlock larger restaurant tiers.'},
  {title:'One kitchen, one team',visual:'👨‍🍳 👩‍🍳 🧑‍🍳',body:'Choose 1, 2 or 3 human players. Every unused crew slot becomes an autonomous bot. Bots now take customer orders, collect real groceries, prepare food at the same stations as humans, cook, assemble, serve and clean.'},
  {title:'Move, interact and throw',visual:'keys',body:'Move to a station and hold your Interact button to work. Throw transfers ingredients quickly across the room. Dash is useful, but positioning still matters.'},
  {title:'Fast service restores lives',visual:'🧾 ⏱️ ❤️',body:'A waiter or crew member must first take the customer’s order. The 30-second FAST SERVICE clock begins only after that order is confirmed. Complete the whole table in time and one lost life is restored. Slow service lowers the bonus; walkouts reduce satisfaction and cost a life.'},
  {title:'Food has real states',visual:'🥔 → 🔪 → 🍟',body:'Ingredients are physical objects. Chop what needs preparation, cook or fry the correct items, assemble them on clean plates, and bake recipes that require the oven.'},
  {title:'Invest before service',visual:'💵 🧱 🔥',body:'Fixtures are business assets bought with persistent cash. More tables and duplicate kitchen stations become available as the restaurant expands. Groceries come from a single professional market wall with fresh, chilled, frozen and pantry compartments.'},
  {title:'Satisfaction unlocks growth',visual:'😊 📈 🏆',body:'Satisfied customers increase your rating. Expansion requires both cash and a satisfaction target, so rushing volume while neglecting service can block future growth.'},
  {title:'Roles and camera',visual:'🔥 🔪 🍽 🎥',body:'Assign Head Chef, Prep Specialist or Service Captain, then use right-drag to orbit, wheel to zoom, C to cycle camera presets and Home to reset.'},
];

const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const lerp=(a,b,t)=>a+(b-a)*t;
const dist2=(a,b)=>Math.hypot(a.x-b.x,a.z-b.z);
const signature=(item)=>`${item.kind}:${item.state}`;
const fmtTime=(s)=>`${String(Math.max(0,Math.floor(s/60))).padStart(2,'0')}:${String(Math.max(0,Math.floor(s%60))).padStart(2,'0')}`;
const snap=(v,g=.5)=>Math.round(v/g)*g;

class Sfx {
  constructor(){this.ctx=null;}
  tone(freq=440,dur=.07,type='sine',gain=.04){try{if(!this.ctx)this.ctx=new(window.AudioContext||window.webkitAudioContext)();const o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type=type;o.frequency.value=freq;g.gain.value=gain;o.connect(g);g.connect(this.ctx.destination);o.start();g.gain.exponentialRampToValueAtTime(.0001,this.ctx.currentTime+dur);o.stop(this.ctx.currentTime+dur);}catch(_){}}
  pickup(){this.tone(520,.06,'triangle');} chop(){this.tone(205,.035,'square',.025);} cook(){this.tone(350,.05,'sine',.022);} serve(){this.tone(620,.08,'triangle');setTimeout(()=>this.tone(830,.1,'triangle'),65);} bad(){this.tone(130,.16,'sawtooth',.04);} order(){this.tone(760,.05,'square',.024);setTimeout(()=>this.tone(930,.06,'square',.024),50);} build(){this.tone(410,.045,'triangle',.02);}
}

class InputManager {
  constructor(){
    this.keys=new Set();
    this.layouts=[
      {up:'KeyW',down:'KeyS',left:'KeyA',right:'KeyD',interact:'KeyE',throw:'KeyQ',dash:'ShiftLeft'},
      {up:'ArrowUp',down:'ArrowDown',left:'ArrowLeft',right:'ArrowRight',interact:'Enter',throw:'Slash',dash:'ShiftRight'},
      {up:'KeyI',down:'KeyK',left:'KeyJ',right:'KeyL',interact:'KeyO',throw:'KeyU',dash:'KeyP'},
    ];
    window.addEventListener('keydown',e=>{this.keys.add(e.code);if(['ArrowUp','ArrowDown','ArrowLeft','ArrowRight','Space'].includes(e.code))e.preventDefault();});
    window.addEventListener('keyup',e=>this.keys.delete(e.code));
  }
  pollGamepad(index){const pads=navigator.getGamepads?navigator.getGamepads():[];const p=pads[index];if(!p)return null;const dz=.2,ax=Math.abs(p.axes[0]||0)>dz?p.axes[0]:0,ay=Math.abs(p.axes[1]||0)>dz?p.axes[1]:0;return{x:ax,y:ay,interact:!!p.buttons[0]?.pressed,throw:!!p.buttons[2]?.pressed,dash:!!p.buttons[1]?.pressed};}
  state(i){const l=this.layouts[i],gp=this.pollGamepad(i);let x=(this.keys.has(l.right)?1:0)-(this.keys.has(l.left)?1:0),y=(this.keys.has(l.down)?1:0)-(this.keys.has(l.up)?1:0);if(gp&&(Math.abs(gp.x)+Math.abs(gp.y)>.05)){x=gp.x;y=gp.y;}return{x,y,interact:this.keys.has(l.interact)||!!gp?.interact,throw:this.keys.has(l.throw)||!!gp?.throw,dash:this.keys.has(l.dash)||!!gp?.dash};}
}

function mat(color,rough=.78,metal=0,emissive=0){return new THREE.MeshStandardMaterial({color,roughness:rough,metalness:metal,emissive,emissiveIntensity:emissive?0.22:0});}
function mesh(geo,material,x=0,y=0,z=0){const m=new THREE.Mesh(geo,material);m.position.set(x,y,z);m.castShadow=true;m.receiveShadow=true;return m;}
function rounded(w,h,d,r=.1,s=3){return new RoundedBoxGeometry(w,h,d,s,r);}
function makeLabel(text,bg='#27343b',fg='#fff',wide=360){const c=document.createElement('canvas');c.width=wide;c.height=88;const x=c.getContext('2d');x.fillStyle=bg;x.beginPath();x.roundRect(5,5,wide-10,78,18);x.fill();x.fillStyle=fg;x.font='900 29px system-ui';x.textAlign='center';x.textBaseline='middle';x.fillText(text,wide/2,44);const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;const sp=new THREE.Sprite(new THREE.SpriteMaterial({map:t,transparent:true,depthTest:false}));sp.scale.set(2.55,.63,1);return sp;}

class AssetFactory {
  constructor(){this.skin=[0xf2bd95,0xd99a73,0x9a624a,0xe7ae84,0x7a4934,0xc98464];this.hair=[0x2c211d,0x543720,0x16191b,0x76543c,0xa06e36];}
  chef(color,role='chef',index=0,isBot=false){
    const g=new THREE.Group(),skin=this.skin[index%this.skin.length],dark=mat(0x26343a),skinMat=mat(skin),white=mat(0xfffdf6),accent=mat(color);
    const ring=mesh(new THREE.TorusGeometry(.47,.055,8,28),new THREE.MeshBasicMaterial({color,transparent:true,opacity:.75}),0,.045,0);ring.rotation.x=Math.PI/2;g.add(ring);
    const torso=mesh(rounded(.65,.82,.43,.18),white,0,.92,0);g.add(torso);
    const sideA=mesh(rounded(.14,.72,.445,.06),accent,-.26,.92,0);const sideB=sideA.clone();sideB.position.x=.26;g.add(sideA,sideB);
    const apron=mesh(rounded(.44,.53,.035,.05),mat(0xf3eee4),0,.83,.235);g.add(apron);
    const belt=mesh(rounded(.5,.07,.05,.025),dark,0,.67,.245);g.add(belt);
    for(const y of[.93,1.08])g.add(mesh(new THREE.SphereGeometry(.025,8,8),dark,0,y,.235));
    const neck=mesh(new THREE.CylinderGeometry(.09,.1,.12,12),skinMat,0,1.36,0);g.add(neck);
    const head=mesh(new THREE.SphereGeometry(.3,22,16),skinMat,0,1.55,0);head.scale.y=1.05;g.add(head);
    const nose=mesh(new THREE.SphereGeometry(.045,10,8),skinMat,0,1.53,.286);nose.scale.set(.8,1,1.15);g.add(nose);
    const eyeMat=new THREE.MeshBasicMaterial({color:0x20272a});for(const x of[-.105,.105]){g.add(mesh(new THREE.SphereGeometry(.027,8,8),eyeMat,x,1.59,.272));const brow=mesh(rounded(.09,.018,.015,.006),dark,x,1.67,.275);brow.rotation.z=x>0?.08:-.08;g.add(brow);}
    const hair=mesh(new THREE.SphereGeometry(.305,18,12),mat(this.hair[index%this.hair.length]),0,1.69,-.02);hair.scale.y=.52;g.add(hair);
    const hatBase=mesh(new THREE.CylinderGeometry(.31,.31,.17,22),white,0,1.83,0);g.add(hatBase);for(const x of[-.18,0,.18]){const puff=mesh(new THREE.SphereGeometry(.18,14,10),white,x,1.98,0);puff.scale.y=.8;g.add(puff);}
    const sleeveMat=white;const leftSleeve=mesh(new THREE.CapsuleGeometry(.11,.26,5,9),sleeveMat,-.43,1.02,0);const rightSleeve=leftSleeve.clone();rightSleeve.position.x=.43;g.add(leftSleeve,rightSleeve);
    const leftHand=mesh(new THREE.SphereGeometry(.105,12,10),skinMat,-.43,.78,.02);const rightHand=leftHand.clone();rightHand.position.x=.43;g.add(leftHand,rightHand);
    const leftLeg=mesh(new THREE.CapsuleGeometry(.105,.37,5,9),dark,-.18,.32,0);const rightLeg=leftLeg.clone();rightLeg.position.x=.18;g.add(leftLeg,rightLeg);
    const shoeL=mesh(rounded(.22,.12,.34,.07),mat(0x151b1e),-.18,.075,.08);const shoeR=shoeL.clone();shoeR.position.x=.18;g.add(shoeL,shoeR);
    const roleLabel=makeLabel(`${isBot?'BOT • ':''}${ROLE_META[role].icon} ${ROLE_META[role].name.toUpperCase()}`,isBot?'#31505b':'#27343b','#fff',520);roleLabel.position.set(0,2.43,0);roleLabel.scale.set(2.45,.41,1);g.add(roleLabel);
    if(role==='chef'){const pan=mesh(new THREE.CylinderGeometry(.12,.12,.035,16),mat(0x3b464a,.3,.7),.53,.82,.03);pan.rotation.z=Math.PI/2;g.add(pan);}else if(role==='prep'){const knife=mesh(rounded(.28,.025,.055,.01),mat(0xaeb9bc,.25,.7),.52,.83,.04);knife.rotation.z=-.35;g.add(knife);}else{const towel=mesh(rounded(.16,.25,.025,.03),mat(0xf2c85c),.48,.83,.03);g.add(towel);}
    g.userData.limbs={leftSleeve,rightSleeve,leftLeg,rightLeg,leftHand,rightHand};g.scale.setScalar(1.14);return g;
  }
  customer(index=0,type=CUSTOMER_TYPES[0]){
    const g=new THREE.Group(),skin=this.skin[index%this.skin.length],skinMat=mat(skin),shirt=mat(type.color),pants=mat([0x293742,0x4b443e,0x344b5a][index%3]);
    const torso=mesh(rounded(.56,.75,.38,.17),shirt,0,.78,0);g.add(torso);const collar=mesh(rounded(.25,.08,.04,.03),mat(0xf5eee3),0,1.08,.21);g.add(collar);
    const head=mesh(new THREE.SphereGeometry(.265,20,14),skinMat,0,1.34,0);g.add(head);const nose=mesh(new THREE.SphereGeometry(.037,8,7),skinMat,0,1.33,.255);g.add(nose);
    const hairStyle=index%3;if(hairStyle===0){const h=mesh(new THREE.SphereGeometry(.27,16,11),mat(this.hair[index%this.hair.length]),0,1.46,-.03);h.scale.y=.58;g.add(h);}else if(hairStyle===1){for(const x of[-.14,0,.14])g.add(mesh(new THREE.SphereGeometry(.13,12,8),mat(this.hair[index%this.hair.length]),x,1.49,-.04));}else{const h=mesh(rounded(.52,.12,.37,.08),mat(this.hair[index%this.hair.length]),0,1.48,-.03);g.add(h);}
    const eyeMat=new THREE.MeshBasicMaterial({color:0x20272a});for(const x of[-.085,.085])g.add(mesh(new THREE.SphereGeometry(.02,7,7),eyeMat,x,1.36,.244));
    if(index%4===0){const glassMat=mat(0x26363d,.3,.55);for(const x of[-.09,.09]){const rim=mesh(new THREE.TorusGeometry(.07,.012,6,16),glassMat,x,1.36,.258);rim.rotation.y=0;g.add(rim);}g.add(mesh(rounded(.08,.015,.012,.005),glassMat,0,1.36,.26));}
    const lArm=mesh(new THREE.CapsuleGeometry(.075,.29,4,8),skinMat,-.35,.78,0);const rArm=lArm.clone();rArm.position.x=.35;g.add(lArm,rArm);const lLeg=mesh(new THREE.CapsuleGeometry(.08,.31,4,8),pants,-.14,.24,0);const rLeg=lLeg.clone();rLeg.position.x=.14;g.add(lLeg,rLeg);
    if(type.id==='student'){const bag=mesh(rounded(.22,.32,.13,.05),mat(0xd3a33e),-.31,.69,-.18);g.add(bag);}if(type.id==='foodie'){const scarf=mesh(new THREE.TorusGeometry(.18,.035,7,18),mat(0xd96655),0,1.13,0);scarf.rotation.x=Math.PI/2;g.add(scarf);}
    g.userData.limbs={lArm,rArm,lLeg,rLeg};g.scale.setScalar(1.07);return g;
  }
  ingredient(kind,state='raw'){
    const g=new THREE.Group();let m;
    if(kind==='tomato'){m=mesh(new THREE.SphereGeometry(.2,18,12),mat(state==='burnt'?0x291d19:state==='chopped'?0xef5b4e:0xe4433e),0,.18,0);const stem=mesh(new THREE.ConeGeometry(.08,.12,7),mat(0x4f8b42),0,.34,0);g.add(stem);}
    if(kind==='lettuce'){m=new THREE.Group();for(let i=0;i<7;i++){const p=mesh(new THREE.SphereGeometry(.13,12,8),mat(state==='burnt'?0x253126:0x58a85d),(i%3-1)*.12,.12+Math.floor(i/3)*.05,(i%2-.5)*.13);p.scale.y=.72;m.add(p);}}
    if(kind==='meat'){m=mesh(new THREE.CylinderGeometry(.2,.2,.1,20),mat(state==='burnt'?0x1f1714:state==='cooked'?0x70402d:0xb65a54),0,.1,0);if(state==='cooked'){for(const z of[-.07,.07]){const line=mesh(rounded(.28,.012,.025,.006),mat(0x3b231c),0,.16,z);line.rotation.y=.35;g.add(line);}}}
    if(kind==='potato'){m=mesh(new THREE.SphereGeometry(.18,16,10),mat(state==='burnt'?0x2c2215:state==='fried'?0xe8b13d:state==='chopped'?0xd7ac67:0xb98850),0,.15,0);m.scale.set(1.25,.8,.95);}
    if(kind==='dough'){m=mesh(new THREE.CylinderGeometry(.23,.23,.07,22),mat(0xe5c596),0,.075,0);}
    if(kind==='cheese'){m=mesh(rounded(.3,.09,.24,.03),mat(0xf1c83a),0,.09,0);for(let i=0;i<3;i++)g.add(mesh(new THREE.SphereGeometry(.025,7,6),mat(0xc99f2a),-.08+i*.08,.14,.1));}
    if(kind==='bun'){m=mesh(new THREE.SphereGeometry(.22,18,11),mat(0xd89a4b),0,.15,0);m.scale.y=.58;for(const x of[-.09,0,.09])g.add(mesh(new THREE.SphereGeometry(.012,6,5),mat(0xf2d7a0),x,.25,.16));}
    if(m)g.add(m);g.scale.setScalar(1.16);return g;
  }
  plate(dirty=false){const g=new THREE.Group(),p=mesh(new THREE.CylinderGeometry(.34,.39,.06,30),mat(dirty?0xaea391:0xf9f8ef,.42),0,.055,0);g.add(p);const rim=mesh(new THREE.TorusGeometry(.31,.025,8,28),mat(dirty?0x998d7d:0xe4e5df),0,.095,0);rim.rotation.x=Math.PI/2;g.add(rim);if(dirty)for(let i=0;i<4;i++)g.add(mesh(new THREE.SphereGeometry(.045,8,6),mat(0x7a4d34),-.14+i*.09,.11,(i%2)*.1-.05));return g;}
  station(type,accent=COLORS.orange){
    const g=new THREE.Group(),base=mesh(rounded(1.35,.9,1.12,.13),mat(COLORS.counter),0,.45,0),edge=mesh(rounded(1.43,.13,1.2,.04),mat(COLORS.trim),0,.93,0);g.add(base,edge);
    const drawer=mesh(rounded(.78,.28,.04,.03),mat(0xd7d3c9),0,.45,.58);g.add(drawer);for(const x of[-.22,.22])g.add(mesh(new THREE.CylinderGeometry(.025,.025,.08,10),mat(0x66767a,.3,.7),x,.45,.62));
    if(type==='prep'){g.add(mesh(rounded(.88,.07,.62,.04),mat(0xc79262),0,1.03,0));const knife=mesh(rounded(.52,.035,.075,.02),mat(0xb7c0c2,.24,.7),0,1.085,.04);knife.rotation.y=.38;g.add(knife);}
    if(type==='stove'){for(const x of[-.31,.31]){const ring=mesh(new THREE.TorusGeometry(.21,.035,8,20),mat(0x20282b,.3,.8),x,1.03,0);ring.rotation.x=Math.PI/2;g.add(ring);}const back=mesh(rounded(1.0,.36,.08,.04),mat(0x5c696d,.28,.5),0,1.14,-.52);g.add(back);}
    if(type==='fryer'){for(const x of[-.26,.26]){const bin=mesh(rounded(.42,.24,.56,.05),mat(0x6f7c80,.25,.7),x,1.02,0);g.add(bin);const oil=mesh(rounded(.34,.028,.43,.02),mat(0xd5a236,.5),x,1.15,0);g.add(oil);}}
    if(type==='oven'){const oven=mesh(rounded(.92,.66,.13,.07),mat(0x3f4d52,.35,.45),0,.5,.6);g.add(oven);const glass=mesh(rounded(.68,.38,.03,.04),mat(0x21333c,.2,.5),0,.52,.69);g.add(glass);for(const x of[-.24,0,.24])g.add(mesh(new THREE.SphereGeometry(.04,8,8),mat(0x20282b),x,.88,.68));}
    if(type==='sink'){const basin=mesh(rounded(.78,.2,.6,.06),mat(0x8fa3a7,.22,.75),0,1.02,0);g.add(basin);const water=mesh(rounded(.65,.025,.47,.02),mat(COLORS.water,.25),0,1.13,0);g.add(water);const faucet=mesh(new THREE.TorusGeometry(.2,.035,8,18,Math.PI),mat(0xaab6b8,.2,.8),0,1.32,-.1);faucet.rotation.z=Math.PI/2;g.add(faucet);}
    if(type==='plate'){for(let i=0;i<5;i++){const p=this.plate(false);p.position.set(0,1.01+i*.05,0);p.scale.set(.8,.8,.8);g.add(p);}}
    if(type==='trash'){const can=mesh(new THREE.CylinderGeometry(.38,.32,.66,20),mat(0x59676a,.55,.25),0,.53,0);g.add(can);const lid=mesh(new THREE.CylinderGeometry(.4,.4,.09,20),mat(0x39484c),0,.9,0);g.add(lid);}
    const stripe=mesh(rounded(1.08,.085,.05,.03),mat(accent),0,.58,.605);g.add(stripe);g.scale.setScalar(1.08);return g;
  }
  crate(kind){const g=new THREE.Group(),box=mesh(rounded(.98,.62,.88,.08),mat(0x9a6a41),0,.32,0);g.add(box);const slat=mat(0xc18b54);for(const z of[-.34,0,.34])g.add(mesh(rounded(1.01,.065,.07,.02),slat,0,.48,z));for(let i=0;i<3;i++){const item=this.ingredient(kind);item.position.set((i-1)*.24,.7,(i%2-.5)*.18);item.scale.multiplyScalar(.75);g.add(item);}g.scale.setScalar(1.07);return g;}
  grocery(kinds=[]){
    const g=new THREE.Group(),frame=mat(0x314950,.28,.72),inside=mat(0xeaf1ee),glass=new THREE.MeshPhysicalMaterial({color:0xa9d7e2,roughness:.08,metalness:0,transparent:true,opacity:.22,transmission:.12,depthWrite:false}),cold=mat(0x77b8cc,.2,.75),warm=mat(0xd8a95f,.25,.75);
    const base=mesh(rounded(5.0,.18,1.18,.08),frame,0,.11,0);g.add(base);
    const body=mesh(rounded(5.0,2.15,1.08,.11),inside,0,1.25,0);g.add(body);
    const top=mesh(rounded(5.08,.18,1.18,.06),frame,0,2.38,0);g.add(top);
    for(const x of[-1.67,0,1.67]){const divider=mesh(rounded(.08,2.02,.98,.025),frame,x,1.28,0);g.add(divider);}
    for(const y of[.72,1.35,1.98]){const shelf=mesh(rounded(4.78,.055,.82,.02),mat(0xb6c4c2,.18,.65),0,y,0);g.add(shelf);}
    const headers=[['FRESH',-1.67,0x5b9d68],['CHILLED / FROZEN',0,0x5d9fbe],['PANTRY',1.67,0xc68f49]];
    for(const [txt,x,c] of headers){const h=mesh(rounded(1.52,.28,.07,.04),mat(c),x,2.18,.57);g.add(h);const lab=makeLabel(txt,`#${c.toString(16).padStart(6,'0')}`,'#fff',360);lab.position.set(x,2.2,.62);lab.scale.set(1.25,.28,1);g.add(lab);}
    const active=kinds.length?kinds:['tomato','lettuce','meat','bun'];
    const slots=[[-2.05,1.05],[-1.23,1.05],[-.41,1.05],[.41,1.05],[1.23,1.05],[2.05,1.05],[-1.58,1.7],[0,1.7],[1.58,1.7]];
    active.slice(0,slots.length).forEach((kind,i)=>{const [x,y]=slots[i],item=this.ingredient(kind);item.position.set(x,y,.38);item.scale.multiplyScalar(.82);g.add(item);const lab=makeLabel(INGREDIENT_META[kind]?.name?.toUpperCase()||kind.toUpperCase(),'#f9fbfa','#26363d',300);lab.position.set(x,y-.28,.58);lab.scale.set(.72,.19,1);g.add(lab);});
    for(const x of[-1.67,0,1.67]){const door=mesh(rounded(1.55,1.87,.035,.035),glass,x,1.36,.585);g.add(door);const handle=mesh(rounded(.045,.54,.04,.018),mat(0x63777c,.22,.78),x+.55,1.36,.62);g.add(handle);}
    const freezerStrip=mesh(rounded(1.52,.07,.055,.025),cold,0,.48,.61);g.add(freezerStrip);const pantryStrip=mesh(rounded(1.52,.07,.055,.025),warm,1.67,.48,.61);g.add(pantryStrip);
    const sign=makeLabel('ROBLEDO GROCERY MARKET','#26363d','#fff',620);sign.position.set(0,2.62,.03);sign.scale.set(3.55,.43,1);g.add(sign);
    g.scale.setScalar(1.04);return g;
  }
  counter(){const g=new THREE.Group();g.add(mesh(rounded(1.35,.9,1.12,.13),mat(COLORS.counter),0,.45,0));g.add(mesh(rounded(1.43,.13,1.2,.04),mat(COLORS.trim),0,.93,0));g.add(mesh(rounded(.85,.28,.04,.03),mat(0xd8d4c9),0,.47,.58));g.scale.setScalar(1.08);return g;}
  table(index=0){const g=new THREE.Group(),top=mesh(new THREE.CylinderGeometry(.85,.85,.13,30),mat(COLORS.wood),0,.78,0);g.add(top);const cloth=mesh(new THREE.CylinderGeometry(.72,.72,.02,30),mat(index%2?0xe8eee8:0xfff5e4),0,.855,0);g.add(cloth);g.add(mesh(new THREE.CylinderGeometry(.13,.17,.7,18),mat(0x6b5749),0,.39,0));const chairMat=mat(index%2?0x617b72:0x826d5c);for(const a of[0,Math.PI/2,Math.PI,Math.PI*1.5]){const c=new THREE.Group();c.add(mesh(rounded(.54,.13,.5,.055),chairMat,0,.44,0));c.add(mesh(rounded(.54,.59,.1,.055),chairMat,0,.76,-.22));for(const x of[-.19,.19])c.add(mesh(new THREE.CylinderGeometry(.035,.04,.42,8),chairMat,x,.22,0));c.position.set(Math.sin(a)*1.15,0,Math.cos(a)*1.15);c.rotation.y=a;g.add(c);}const vase=mesh(new THREE.CylinderGeometry(.08,.12,.25,12),mat(0xd8ddd5),0,1.0,0);g.add(vase);const flower=mesh(new THREE.SphereGeometry(.09,10,8),mat(index%2?0xe0645c:0xe9b541),0,1.18,0);g.add(flower);const label=makeLabel(`TABLE ${index+1}`,'#fff7e8','#26343a');label.position.set(0,1.78,0);label.scale.multiplyScalar(.68);g.add(label);g.scale.setScalar(1.08);return g;}
  plant(){const g=new THREE.Group();g.add(mesh(new THREE.CylinderGeometry(.28,.35,.45,16),mat(0xb2704d),0,.22,0));for(let i=0;i<7;i++){const leaf=mesh(new THREE.SphereGeometry(.2,10,8),mat(0x4d9061),(i%3-1)*.2,.62+Math.floor(i/3)*.16,(i%2-.5)*.18);leaf.scale.set(.65,1.4,.5);leaf.rotation.z=(i%3-1)*.35;g.add(leaf);}return g;}
}

class CameraRig {
  constructor(game){this.game=game;this.mode='dynamic';this.yaw=Math.PI*.25;this.pitch=.72;this.distance=18;this.target=new THREE.Vector3(0,0,1);this.smoothTarget=this.target.clone();this.prevTarget=this.target.clone();this.lean=0;this.dragging=false;this.lastX=0;this.lastY=0;const c=game.canvas;c.addEventListener('contextmenu',e=>e.preventDefault());c.addEventListener('pointerdown',e=>{if(e.button===2){this.dragging=true;this.lastX=e.clientX;this.lastY=e.clientY;c.setPointerCapture?.(e.pointerId);}});c.addEventListener('pointerup',e=>{if(e.button===2)this.dragging=false;});c.addEventListener('pointermove',e=>{if(!this.dragging)return;const dx=e.clientX-this.lastX,dy=e.clientY-this.lastY;this.lastX=e.clientX;this.lastY=e.clientY;this.yaw-=dx*.006;this.pitch=clamp(this.pitch+dy*.004,.38,1.25);});c.addEventListener('wheel',e=>{e.preventDefault();this.distance=clamp(this.distance+e.deltaY*.012,16,34);},{passive:false});}
  setMode(mode){this.mode=mode;document.getElementById('camera-mode-label').textContent=mode.toUpperCase();const base=18+(this.game.business?.expansionTier||0)*3;if(mode==='dynamic'){this.pitch=.68;this.distance=base;}if(mode==='classic'){this.yaw=Math.PI*.25;this.pitch=.72;this.distance=base+1;}if(mode==='top'){this.pitch=1.18;this.distance=base+5;}}
  cycle(){const modes=['dynamic','classic','top'];this.setMode(modes[(modes.indexOf(this.mode)+1)%modes.length]);}
  reset(){const base=18+(this.game.business?.expansionTier||0)*3;this.yaw=Math.PI*.25;this.pitch=this.mode==='top'?1.18:this.mode==='dynamic'?.68:.72;this.distance=this.mode==='top'?base+5:this.mode==='classic'?base+1:base;}
  update(dt){const g=this.game;let target=new THREE.Vector3(0,0,1);if(g.state==='playing'&&g.players.length){for(const p of g.players)target.add(p.group.position);target.multiplyScalar(1/g.players.length);target.y=.6;}else if(g.state==='build'){target.set(0,.4,0);}this.target.copy(target);const k=1-Math.exp(-dt*4.5);this.smoothTarget.lerp(target,k);const vel=target.clone().sub(this.prevTarget).multiplyScalar(1/Math.max(dt,.016));this.prevTarget.copy(target);if(this.mode==='dynamic'){const localSide=vel.x*Math.cos(this.yaw)-vel.z*Math.sin(this.yaw);this.lean=lerp(this.lean,clamp(-localSide*.008,-.035,.035),1-Math.exp(-dt*3));}else this.lean=lerp(this.lean,0,1-Math.exp(-dt*4));const cp=Math.cos(this.pitch),sp=Math.sin(this.pitch);const d=this.distance;const pos=new THREE.Vector3(Math.sin(this.yaw)*cp*d,sp*d,Math.cos(this.yaw)*cp*d).add(this.smoothTarget);g.camera.position.lerp(pos,1-Math.exp(-dt*6));g.camera.lookAt(this.smoothTarget);g.camera.rotation.z+=this.lean;}
}

class WorldItem {
  constructor(game,kind,state='raw',isPlate=false){this.game=game;this.kind=kind;this.state=state;this.isPlate=isPlate;this.components=[];this.baked=false;this.burnt=false;this.dirty=false;this.group=isPlate?game.assets.plate(false):game.assets.ingredient(kind,state);this.group.userData.item=this;this.velocity=new THREE.Vector3();this.airborne=false;this.onSurface=false;this.dead=false;game.itemRoot.add(this.group);}
  description(){if(this.isPlate){if(this.dirty)return'dirty plate';const r=identifyRecipe(this);return r?RECIPES[r].name:`plate${this.components.length?' + '+this.components.map(c=>INGREDIENT_META[c.kind]?.name||c.kind).join(', '):''}`;}return`${this.state==='raw'?'':this.state+' '}${INGREDIENT_META[this.kind]?.name||this.kind}`.trim();}
  setState(s){this.state=s;if(!this.isPlate){while(this.group.children.length)this.group.remove(this.group.children[0]);const n=this.game.assets.ingredient(this.kind,s);while(n.children.length)this.group.add(n.children[0]);}}
  setDirty(v){this.dirty=v;if(this.isPlate){while(this.group.children.length)this.group.remove(this.group.children[0]);const n=this.game.assets.plate(v);while(n.children.length)this.group.add(n.children[0]);}}
  dispose(){this.dead=true;this.group.removeFromParent();}
}

function identifyRecipe(plate){if(!plate?.isPlate||plate.dirty||plate.burnt)return null;const sigs=plate.components.map(signature).sort();for(const r of Object.values(RECIPES)){const req=[...r.components].sort();if(req.length!==sigs.length)continue;if(req.every((v,i)=>v===sigs[i])&&!!r.baked===!!plate.baked)return r.id;}return null;}

class CrewMember {
  constructor(game,index,color,start,role='chef',human=true){this.game=game;this.index=index;this.role=role;this.human=human;this.group=game.assets.chef(color,role,index,!human);this.group.position.copy(start);game.playerRoot.add(this.group);this.held=null;this.radius=.43;this.speed=(human?4.55:6.6)*ROLE_META[role].move;this.facing=new THREE.Vector3(0,0,1);this.prevInteract=false;this.prevThrow=false;this.dashCooldown=0;this.walkPhase=0;this.bot=human?null:new BotBrain(game,this);}
  workMultiplier(type){const r=ROLE_META[this.role],ai=this.human?1:1.45;let base=1;if(type==='prep')base=r.prep;else if(['stove','fryer','oven'].includes(type))base=r.cook;else if(type==='sink')base=r.wash;return base*ai;}
  pick(item){if(this.held||!item||item.dead)return false;this.held=item;item.onSurface=false;this.game.sfx.pickup();return true;}
  dropAt(pos){if(!this.held)return;const i=this.held;this.held=null;i.group.position.copy(pos);i.group.position.y=.22;i.onSurface=false;}
  throwItem(){if(!this.held)return;const i=this.held;this.held=null;i.airborne=true;i.onSurface=false;i.group.position.copy(this.group.position).add(new THREE.Vector3(0,1.35,0));i.velocity.copy(this.facing).multiplyScalar(7.2);i.velocity.y=4.0;this.game.sfx.pickup();}
  animate(dt,moving,boost=1){const l=this.group.userData.limbs;if(moving){this.walkPhase+=dt*10*boost;if(l){l.leftSleeve.rotation.x=Math.sin(this.walkPhase)*.58;l.rightSleeve.rotation.x=-Math.sin(this.walkPhase)*.58;l.leftLeg.rotation.x=-Math.sin(this.walkPhase)*.34;l.rightLeg.rotation.x=Math.sin(this.walkPhase)*.34;l.leftHand.rotation.x=l.leftSleeve.rotation.x;l.rightHand.rotation.x=l.rightSleeve.rotation.x;}this.group.position.y=.02+Math.abs(Math.sin(this.walkPhase*2))*.035;}else{if(l)for(const p of Object.values(l))p.rotation.x*=.75;this.group.position.y=0;}if(this.held){this.held.group.position.copy(this.group.position).add(new THREE.Vector3(this.facing.x*.25,1.42,this.facing.z*.25));this.held.group.rotation.y=this.group.rotation.y;}}
  updateHuman(dt,input){this.dashCooldown=Math.max(0,this.dashCooldown-dt);let dx=input.x,dz=input.y;const len=Math.hypot(dx,dz);if(len>.001){dx/=Math.max(1,len);dz/=Math.max(1,len);this.facing.set(dx,0,dz);this.group.rotation.y=Math.atan2(dx,dz);}let boost=input.dash&&this.dashCooldown<=0?1.72:1;if(boost>1)this.dashCooldown=.2;this.game.moveCrew(this,new THREE.Vector3(dx,0,dz).multiplyScalar(this.speed*boost*dt));this.animate(dt,len>.05,boost);if(input.throw&&!this.prevThrow)this.throwItem();this.prevThrow=input.throw;const nearest=this.game.nearestInteractable(this.group.position,1.55);if(input.interact&&nearest)this.game.interact(this,nearest,dt,true);this.prevInteract=input.interact;}
  update(dt){if(this.human)this.updateHuman(dt,this.game.input.state(this.index));else this.bot.update(dt);}
}

class BotBrain {
  constructor(game,member){this.game=game;this.member=member;this.task=null;this.actionIndex=0;this.stall=0;this.processStation=null;this.avoidSign=member.index%2?1:-1;this.id=`bot-${member.index}`;}
  record(type,data={}){this.game.qaRecord(type,{bot:this.id,role:this.member.role,...data});}
  releaseStation(s){if(s?.reservedBy===this.id)s.reservedBy=null;}
  releaseAll(){for(const s of this.game.stations)if(s.reservedBy===this.id)s.reservedBy=null;this.processStation=null;}
  cleanupOwnedItems(){const m=this.member;if(m.held){m.held.dispose();m.held=null;}for(const s of this.game.stations){if(s.reservedBy!==this.id)continue;if(s.slot){s.slot.dispose();s.slot=null;}s.progress=0;s.cook=0;s.ready=false;if(s.bar)s.bar.scale.x=.001;}}
  abort(reason='aborted'){if(this.task?.order&&this.task.order.claimedBy===this.id)this.task.order.claimedBy=null;if(this.task?.party?.orderClaim===this.id)this.task.party.orderClaim=null;if(this.task?.table?.cleanClaim===this.id)this.task.table.cleanClaim=null;this.cleanupOwnedItems();this.releaseAll();if(this.task)this.record('bot-task-abort',{kind:this.task.kind,reason});this.task=null;this.actionIndex=0;this.stall=0;}
  finish(){if(this.task?.order&&this.task.order.claimedBy===this.id)this.task.order.claimedBy=null;if(this.task?.party?.orderClaim===this.id)this.task.party.orderClaim=null;if(this.task?.table?.cleanClaim===this.id)this.task.table.cleanClaim=null;this.releaseAll();this.task=null;this.actionIndex=0;this.stall=0;}
  makeDishActions(recipeId,table){const r=RECIPES[recipeId],actions=[{kind:'getPlate'},{kind:'placePlate'}];for(const sig of r.components){const[k,state]=sig.split(':');actions.push({kind:'storage',ingredient:k});if(state==='chopped')actions.push({kind:'prepPlace',ingredient:k},{kind:'prepWork',ingredient:k},{kind:'prepPick',ingredient:k});if(state==='cooked')actions.push({kind:'cookPlace',station:'stove',ingredient:k},{kind:'cookWait',station:'stove',ingredient:k,want:'cooked'},{kind:'cookPick',station:'stove',ingredient:k,want:'cooked'});if(state==='fried')actions.push({kind:'prepPlace',ingredient:k},{kind:'prepWork',ingredient:k},{kind:'prepPick',ingredient:k},{kind:'cookPlace',station:'fryer',ingredient:k},{kind:'cookWait',station:'fryer',ingredient:k,want:'fried'},{kind:'cookPick',station:'fryer',ingredient:k,want:'fried'});actions.push({kind:'assemble',ingredient:k});}actions.push({kind:'pickupPlate'});if(r.baked)actions.push({kind:'ovenPlace',station:'oven'},{kind:'ovenWait',station:'oven'},{kind:'ovenPick',station:'oven'});actions.push({kind:'deliver',table});return actions;}
  reserveCounter(){const s=this.game.stations.find(x=>x.type==='counter'&&!x.reservedBy&&!x.slot);if(s)s.reservedBy=this.id;return s||null;}
  chooseTask(){const g=this.game,m=this.member,role=ROLE_META[m.role],hasServiceBot=g.players.some(p=>!p.human&&ROLE_META[p.role]?.botBias==='service');const ready=g.parties.filter(p=>p.state==='readyToOrder'&&!p.orderClaim).sort((a,b)=>b.orderWaitElapsed-a.orderWaitElapsed);if(ready.length&&(role.botBias==='service'||(!hasServiceBot&&ready[0].orderWaitElapsed>3))){const party=ready[0];party.orderClaim=this.id;this.task={kind:'order',party,table:party.table,actions:[{kind:'takeOrder',table:party.table,duration:.8}]};this.record('bot-order-claim',{table:party.table.id+1});return;}
    if(role.botBias==='service'){const dirty=g.tables.find(t=>t.dirty>0&&!t.cleanClaim);if(dirty){dirty.cleanClaim=this.id;this.task={kind:'clean',table:dirty,actions:[{kind:'dirtyPickup',table:dirty},{kind:'sinkPlace',station:'sink'},{kind:'sinkWork',station:'sink'},{kind:'sinkPick',station:'sink'},{kind:'rackReturn'}]};this.record('bot-clean-claim',{table:dirty.id+1});return;}}
    const candidates=[];for(const p of g.parties){if(p.state!=='waiting')continue;for(const o of p.orders)if(!o.claimedBy)candidates.push({party:p,order:o});}
    if(candidates.length){let choice=candidates[0];if(role.botBias==='hot')choice=candidates.find(c=>RECIPES[c.order.recipeId].baked||RECIPES[c.order.recipeId].components.some(x=>x.includes(':cooked')||x.includes(':fried')))||choice;if(role.botBias==='prep')choice=candidates.find(c=>RECIPES[c.order.recipeId].components.some(x=>x.includes(':chopped')))||choice;const counter=this.reserveCounter();if(counter){choice.order.claimedBy=this.id;this.task={kind:'dish',party:choice.party,order:choice.order,recipeId:choice.order.recipeId,table:choice.party.table,counter,actions:this.makeDishActions(choice.order.recipeId,choice.party.table)};this.record('bot-dish-claim',{table:choice.party.table.id+1,recipe:choice.order.recipeId});return;}}
    const dirty=g.tables.find(t=>t.dirty>0&&!t.cleanClaim);if(dirty){dirty.cleanClaim=this.id;this.task={kind:'clean',table:dirty,actions:[{kind:'dirtyPickup',table:dirty},{kind:'sinkPlace',station:'sink'},{kind:'sinkWork',station:'sink'},{kind:'sinkPick',station:'sink'},{kind:'rackReturn'}]};this.record('bot-clean-claim',{table:dirty.id+1});return;}
    if(ready.length&&(role.botBias==='service'||!hasServiceBot)){const party=ready[0];party.orderClaim=this.id;this.task={kind:'order',party,table:party.table,actions:[{kind:'takeOrder',table:party.table,duration:.8}]};this.record('bot-order-claim',{table:party.table.id+1});}
  }
  acquireProcess(type){if(this.processStation&&this.processStation.type===type&&this.processStation.reservedBy===this.id)return this.processStation;const s=this.game.stations.find(x=>x.type===type&&(!x.reservedBy||x.reservedBy===this.id));if(s){s.reservedBy=this.id;this.processStation=s;}return s||null;}
  target(action){const g=this.game;if(['takeOrder','deliver','dirtyPickup'].includes(action.kind))return action.table;if(action.kind==='storage')return g.stations.find(s=>s.type==='storage'&&s.kind===action.ingredient);if(action.kind==='getPlate'||action.kind==='rackReturn')return g.stations.find(s=>s.type==='plate');if(['placePlate','assemble','pickupPlate'].includes(action.kind))return this.task.counter;if(action.kind.startsWith('prep'))return this.acquireProcess('prep');if(action.kind.startsWith('cook'))return this.acquireProcess(action.station);if(action.kind.startsWith('oven'))return this.acquireProcess('oven');if(action.kind.startsWith('sink'))return this.acquireProcess('sink');return null;}
  advance(){this.actionIndex++;this.stall=0;}
  staticBlocked(pos){const m=this.member,g=this.game,b=g.currentBounds();if(pos.x<-b.xMax+.5||pos.x>b.xMax-.5||pos.z<b.zMin+.5||pos.z>b.zMax-.5)return true;for(const o of g.obstacles)if(Math.abs(pos.x-o.x)<o.w/2+m.radius+.06&&Math.abs(pos.z-o.z)<o.d/2+m.radius+.06)return true;return false;}
  pathBlocked(pos){if(this.staticBlocked(pos))return true;const m=this.member;for(const other of this.game.players){if(other===m)continue;if(Math.hypot(pos.x-other.group.position.x,pos.z-other.group.position.z)<m.radius+other.radius+.16)return true;}return false;}
  approachPoint(target){const m=this.member,pos=target?.pos||target?.group?.position;if(!pos||target.type==='storage')return pos;const o=this.game.obstacles.find(x=>x.type===target.type&&Math.abs(x.x-pos.x)<.08&&Math.abs(x.z-pos.z)<.08);if(!o)return pos;const pad=m.radius+.24,candidates=[new THREE.Vector3(o.x,0,o.z-o.d/2-pad),new THREE.Vector3(o.x+o.w/2+pad,0,o.z),new THREE.Vector3(o.x,0,o.z+o.d/2+pad),new THREE.Vector3(o.x-o.w/2-pad,0,o.z)];let valid=candidates.filter(p=>!this.pathBlocked(p));if(!valid.length)valid=candidates.filter(p=>!this.staticBlocked(p));const pool=valid.length?valid:candidates;pool.sort((a,b)=>a.distanceToSquared(m.group.position)-b.distanceToSquared(m.group.position));return pool[0];}
  planPath(goal){const m=this.member,start=m.group.position.clone(),step=.38,gx=Math.round((goal.x-start.x)/step),gz=Math.round((goal.z-start.z)/step),key=(x,z)=>`${x},${z}`,dirs=[[1,0],[-1,0],[0,1],[0,-1]],open=new Map(),came=new Map(),cost=new Map(),nodes=new Map(),startKey=key(0,0);open.set(startKey,{x:0,z:0,f:Math.abs(gx)+Math.abs(gz)});cost.set(startKey,0);nodes.set(startKey,{x:0,z:0});let endKey=null;for(let iter=0;iter<3200&&open.size;iter++){let ck=null,cur=null;for(const[k,n]of open)if(!cur||n.f<cur.f){ck=k;cur=n;}open.delete(ck);if(Math.abs(cur.x-gx)+Math.abs(cur.z-gz)<=1){endKey=ck;break;}for(const[dX,dZ]of dirs){const nx=cur.x+dX,nz=cur.z+dZ,nk=key(nx,nz),p=new THREE.Vector3(start.x+nx*step,0,start.z+nz*step);if(this.pathBlocked(p))continue;const nc=(cost.get(ck)||0)+1;if(nc>=(cost.get(nk)??Infinity))continue;cost.set(nk,nc);came.set(nk,ck);nodes.set(nk,{x:nx,z:nz});open.set(nk,{x:nx,z:nz,f:nc+Math.abs(gx-nx)+Math.abs(gz-nz)});}}if(!endKey)return[];const rev=[];for(let k=endKey;k&&k!==startKey;k=came.get(k)){const n=nodes.get(k);if(!n)break;rev.push(new THREE.Vector3(start.x+n.x*step,0,start.z+n.z*step));}rev.reverse();rev.push(goal.clone());return rev;}
  moveToward(point,dt,scale=.9){const m=this.member,g=this.game,v=new THREE.Vector3(point.x-m.group.position.x,0,point.z-m.group.position.z);if(v.length()<.001)return false;v.normalize();m.facing.copy(v);m.group.rotation.y=Math.atan2(v.x,v.z);const before=m.group.position.clone();g.moveCrew(m,v.clone().multiplyScalar(m.speed*scale*dt));let moved=before.distanceToSquared(m.group.position)>1e-7;if(!moved){const side=new THREE.Vector3(-v.z,0,v.x).multiplyScalar(this.avoidSign);g.moveCrew(m,side.clone().multiplyScalar(m.speed*.68*dt));moved=before.distanceToSquared(m.group.position)>1e-7;if(!moved){this.avoidSign*=-1;g.moveCrew(m,side.multiplyScalar(-m.speed*.68*dt));moved=before.distanceToSquared(m.group.position)>1e-7;}}return moved;}
  near(target,dt){const m=this.member,raw=target?.pos||target?.group?.position;if(!raw)return false;const interactionReach=target.type==='storage'?.9:target.type==='table'?1.48:1.5;if(m.group.position.distanceTo(raw)<=interactionReach){m.animate(dt,false);return true;}if(this.navTarget!==target){this.navTarget=target;this.navGoal=(this.approachPoint(target)||raw).clone();this.navMode='direct';this.navPath=[];this.navProbeTime=0;this.navProbeDist=m.group.position.distanceTo(this.navGoal);this.navPathStall=0;}if(target.type!=='storage'&&this.navGoal&&this.pathBlocked(this.navGoal)){const alt=this.approachPoint(target);if(alt&&alt.distanceToSquared(this.navGoal)>.04){this.navGoal=alt.clone();this.navMode='direct';this.navPath=[];this.navProbeTime=0;this.navProbeDist=m.group.position.distanceTo(this.navGoal);this.navPathProbeTime=0;this.avoidSign*=-1;}}const goal=this.navGoal||raw,goalDist=m.group.position.distanceTo(goal);if(goalDist<.28){m.animate(dt,false);return true;}
    if(this.navMode!=='path'){this.moveToward(goal,dt,.9);const after=m.group.position.distanceTo(goal);this.navProbeTime=(this.navProbeTime||0)+dt;if(this.navProbeTime>=.45){const improvement=(this.navProbeDist??after)-after;if(improvement<.14){this.navPath=this.planPath(goal);if(this.navPath.length){this.navMode='path';this.navPathStall=0;this.navPathProbe=after;this.navPathProbeTime=0;}else this.avoidSign*=-1;}this.navProbeDist=after;this.navProbeTime=0;}m.animate(dt,true,.95);return false;}
    while(this.navPath.length&&m.group.position.distanceTo(this.navPath[0])<.2)this.navPath.shift();if(!this.navPath.length){this.navMode='direct';this.navProbeDist=m.group.position.distanceTo(goal);this.navProbeTime=0;m.animate(dt,true,.95);return false;}const wp=this.navPath[0];this.moveToward(wp,dt,.9);while(this.navPath.length&&m.group.position.distanceTo(this.navPath[0])<.2)this.navPath.shift();const after=m.group.position.distanceTo(goal);this.navPathProbeTime=(this.navPathProbeTime||0)+dt;if(this.navPathProbeTime>=.65){const improvement=(this.navPathProbe??after)-after;if(improvement<.08){const retry=this.planPath(goal);if(retry.length)this.navPath=retry;else{this.navMode='direct';this.avoidSign*=-1;this.navProbeDist=after;this.navProbeTime=0;}}this.navPathProbe=after;this.navPathProbeTime=0;}m.animate(dt,true,.95);return false;}
  update(dt){const m=this.member,g=this.game;if(!this.task)this.chooseTask();if(!this.task){const b=g.currentBounds(),bias=ROLE_META[m.role]?.botBias,candidates=bias==='service'?[new THREE.Vector3(Math.min(3.4,b.xMax-1),0,b.zMax-1.15),new THREE.Vector3(1.4,0,b.zMax-1.0),new THREE.Vector3(3.6,0,1.6)]:bias==='prep'?[new THREE.Vector3(-1.2,0,b.zMax-1.0),new THREE.Vector3(.4,0,b.zMax-1.1),new THREE.Vector3(-1.4,0,1.8)]:[new THREE.Vector3(1.2,0,b.zMax-1.0),new THREE.Vector3(0,0,b.zMax-1.0),new THREE.Vector3(1.5,0,1.8)];const free=candidates.find(p=>!this.pathBlocked(p))||candidates.find(p=>!this.staticBlocked(p))||candidates[0];if(!this.idleTarget||this.idleTarget.pos.distanceToSquared(free)>.04)this.idleTarget={type:'storage',pos:free.clone()};if(!this.near(this.idleTarget,dt))return;m.animate(dt,false);return;}if(this.task.kind==='dish'&&(!g.parties.includes(this.task.party)||this.task.party.state!=='waiting'||!this.task.party.orders.includes(this.task.order))){this.abort('order no longer active');return;}if(this.task.kind==='order'&&this.task.party.state!=='readyToOrder'){this.finish();return;}const a=this.task.actions[this.actionIndex];if(!a){this.finish();return;}const t=this.target(a);if(!t){this.stall+=dt;m.animate(dt,false);if(this.stall>7)this.abort(`station unavailable for ${a.kind}`);return;}this.stall=0;if(!this.near(t,dt))return;
    if(a.kind==='takeOrder'){a.work=(a.work||0)+dt*m.workMultiplier('service');if(a.work>=a.duration){if(this.task.party.confirmOrder(m)){this.record('bot-order-taken',{table:this.task.table.id+1});}this.finish();}return;}
    if(a.kind==='getPlate'){if(!m.held)g.interact(m,t,dt,true);if(m.held?.isPlate&&!m.held.dirty){this.record('bot-plate-pick');this.advance();}return;}
    if(a.kind==='placePlate'){g.interact(m,t,dt,true);if(!m.held&&t.slot?.isPlate){this.record('bot-plate-counter');this.advance();}return;}
    if(a.kind==='storage'){if(!m.held)g.interact(m,t,dt,true);if(m.held?.kind===a.ingredient){this.record('bot-storage-pick',{ingredient:a.ingredient});this.advance();}return;}
    if(a.kind==='prepPlace'){g.interact(m,t,dt,true);if(!m.held&&t.slot?.kind===a.ingredient)this.advance();return;}
    if(a.kind==='prepWork'){g.interact(m,t,dt,true);if(t.slot?.state==='chopped'){this.record('bot-prep-complete',{ingredient:a.ingredient});this.advance();}return;}
    if(a.kind==='prepPick'){g.interact(m,t,dt,true);if(m.held?.kind===a.ingredient&&m.held.state==='chopped'){this.releaseStation(t);this.processStation=null;this.advance();}return;}
    if(a.kind==='cookPlace'){g.interact(m,t,dt,true);if(!m.held&&t.slot?.kind===a.ingredient){this.record('bot-cook-start',{station:a.station,ingredient:a.ingredient});this.advance();}return;}
    if(a.kind==='cookWait'){if(t.slot?.state===a.want&&t.ready){this.record('bot-cook-ready',{station:a.station,ingredient:a.ingredient});this.advance();}return;}
    if(a.kind==='cookPick'){g.interact(m,t,dt,true);if(m.held?.kind===a.ingredient&&m.held.state===a.want){this.releaseStation(t);this.processStation=null;this.advance();}return;}
    if(a.kind==='assemble'){const before=t.slot?.components?.length||0;g.interact(m,t,dt,true);const after=t.slot?.components?.length||0;if(!m.held&&after>before){this.record('bot-assemble',{ingredient:a.ingredient,components:after});this.advance();}return;}
    if(a.kind==='pickupPlate'){if(!m.held)g.interact(m,t,dt,true);if(m.held?.isPlate){this.releaseStation(t);this.record('bot-dish-assembled',{recipe:this.task.recipeId});this.advance();}return;}
    if(a.kind==='ovenPlace'){g.interact(m,t,dt,true);if(!m.held&&t.slot?.isPlate){this.record('bot-cook-start',{station:'oven',recipe:this.task.recipeId});this.advance();}return;}
    if(a.kind==='ovenWait'){if(t.slot?.baked&&t.ready){this.record('bot-cook-ready',{station:'oven',recipe:this.task.recipeId});this.advance();}return;}
    if(a.kind==='ovenPick'){g.interact(m,t,dt,true);if(m.held?.isPlate&&m.held.baked){this.releaseStation(t);this.processStation=null;this.advance();}return;}
    if(a.kind==='deliver'){const before=this.task.party.orders.length;g.interact(m,t,dt,true);if(!m.held&&this.task.party.orders.length<before){this.record('bot-dish-served',{table:t.id+1,recipe:this.task.recipeId});this.finish();}return;}
    if(a.kind==='dirtyPickup'){g.interact(m,t,dt,true);if(m.held?.isPlate&&m.held.dirty){this.record('bot-dirty-pick',{table:t.id+1});this.advance();}return;}
    if(a.kind==='sinkPlace'){g.interact(m,t,dt,true);if(!m.held&&t.slot?.dirty)this.advance();return;}
    if(a.kind==='sinkWork'){g.interact(m,t,dt,true);if(t.slot?.isPlate&&!t.slot.dirty){this.record('bot-wash-complete');this.advance();}return;}
    if(a.kind==='sinkPick'){g.interact(m,t,dt,true);if(m.held?.isPlate&&!m.held.dirty){this.releaseStation(t);this.processStation=null;this.advance();}return;}
    if(a.kind==='rackReturn'){g.interact(m,t,dt,true);if(!m.held){this.record('bot-clean-cycle',{table:this.task.table.id+1});this.finish();}}
  }
}

class CustomerParty {
  constructor(game,table,size,recipes,patience,type){
    this.game=game;this.id=++game.partySerial;this.table=table;this.size=size;this.type=type;
    this.orders=Array.from({length:size},(_,i)=>({id:`${this.id}-${i}`,recipeId:recipes[Math.floor(Math.random()*recipes.length)],claimedBy:null}));
    this.originalRecipes=this.orders.map(o=>o.recipeId);this.patienceMax=patience*type.patience+Math.random()*8;this.patience=this.patienceMax;
    this.state='entering';this.browse=2.0+Math.random()*2.2;this.orderWaitElapsed=0;this.waitElapsed=0;this.eat=0;this.orderClaim=null;this.orderTakenBy=null;this.customers=[];
    const door=game.entrancePoint();for(let i=0;i<size;i++){const c=game.assets.customer((game.customerSerial+i)%12,type);c.position.copy(door).add(new THREE.Vector3(i*.28,0,i*.22));game.customerRoot.add(c);this.customers.push({group:c,target:table.seats[i].clone(),speed:2.2+Math.random()*.35,phase:Math.random()*6});}game.customerSerial+=size;
  }
  animateCustomer(c,dt,moving){const limbs=c.group.userData.limbs;c.phase+=dt*9;if(limbs&&moving){limbs.lArm.rotation.x=Math.sin(c.phase)*.45;limbs.rArm.rotation.x=-Math.sin(c.phase)*.45;limbs.lLeg.rotation.x=-Math.sin(c.phase)*.28;limbs.rLeg.rotation.x=Math.sin(c.phase)*.28;}else if(limbs)for(const l of Object.values(limbs))l.rotation.x*=.8;}
  update(dt){
    if(this.state==='entering'){let arrived=true;for(const c of this.customers){const v=c.target.clone().sub(c.group.position);v.y=0;if(v.length()>.1){arrived=false;v.normalize();c.group.position.addScaledVector(v,c.speed*dt);c.group.rotation.y=Math.atan2(v.x,v.z);this.animateCustomer(c,dt,true);}else{c.group.position.copy(c.target);c.group.position.y=-.08;c.group.rotation.y=Math.atan2(this.table.pos.x-c.group.position.x,this.table.pos.z-c.group.position.z);this.animateCustomer(c,dt,false);}}if(arrived)this.state='browsing';}
    else if(this.state==='browsing'){this.browse-=dt;if(this.browse<=0){this.state='readyToOrder';this.orderWaitElapsed=0;this.game.qaRecord('party-ready-order',{table:this.table.id+1});this.game.sfx.order();this.game.flash(`${this.type.label} at Table ${this.table.id+1} is ready — a waiter must TAKE THE ORDER.`);}}
    else if(this.state==='readyToOrder'){this.orderWaitElapsed+=dt;this.patience-=dt*.55;if(this.patience<=0)this.game.walkout(this);}
    else if(this.state==='waiting'){this.waitElapsed+=dt;this.patience-=dt;if(this.patience<=0)this.game.walkout(this);}
    else if(this.state==='eating'){this.eat-=dt;if(this.eat<=0)this.beginLeaving();}
    else if(this.state==='leaving'){let done=true;const exit=this.game.exitPoint();for(const c of this.customers){const v=exit.clone().sub(c.group.position);v.y=0;if(v.length()>.25){done=false;v.normalize();c.group.position.addScaledVector(v,c.speed*dt);c.group.rotation.y=Math.atan2(v.x,v.z);this.animateCustomer(c,dt,true);}}if(done){for(const c of this.customers)c.group.removeFromParent();this.table.party=null;this.table.dirty=Math.max(this.table.dirty,this.size);this.state='gone';}}
  }
  confirmOrder(server=null){if(this.state!=='readyToOrder')return false;this.state='waiting';this.waitElapsed=0;this.orderTakenBy=server?(server.human?`P${server.index+1}`:`BOT ${server.index+1}`):'CREW';this.orderClaim=null;this.game.qaRecord('order-confirmed',{table:this.table.id+1,by:this.orderTakenBy,recipes:this.orders.map(o=>o.recipeId)});this.game.sfx.order();this.game.flash(`${this.orderTakenBy} took Table ${this.table.id+1} order — FAST LIFE ≤ ${FAST_SERVICE_SECONDS}s`);return true;}
  serve(recipeId,server=null){
    const claim=server&&!server.human?server.bot?.id:null;let idx=claim?this.orders.findIndex(o=>o.recipeId===recipeId&&o.claimedBy===claim):-1;if(idx<0)idx=this.orders.findIndex(o=>o.recipeId===recipeId);if(idx<0)return false;const[o]=this.orders.splice(idx,1);o.claimedBy=null;
    if(this.orders.length===0){
      const urgency=this.patience/this.patienceMax,tipMult=server?ROLE_META[server.role].tip:1,tip=Math.round((95+175*urgency*this.game.combo)*this.type.tip*tipMult);
      this.game.score+=tip;this.game.combo=Math.min(9,this.game.combo+1);this.state='eating';this.eat=6+Math.random()*3;this.game.sfx.serve();this.game.successfulTables++;
      this.game.completeTable(this,server,tip);
    }else{this.game.score+=Math.round(RECIPES[recipeId].price*.35);this.game.sfx.serve();this.game.flash(`${RECIPES[recipeId].name} served to Table ${this.table.id+1}`);}
    return true;
  }
  beginLeaving(){this.state='leaving';}
  forceLeave(){for(const o of this.orders)o.claimedBy=null;this.orderClaim=null;this.state='leaving';}
}

class Game {
  constructor(){
    this.canvas=document.getElementById('game-canvas');this.renderer=new THREE.WebGLRenderer({canvas:this.canvas,antialias:true,alpha:false,powerPreference:'high-performance'});this.renderer.setPixelRatio(Math.min(2,devicePixelRatio));this.renderer.shadowMap.enabled=true;this.renderer.shadowMap.type=THREE.PCFSoftShadowMap;this.renderer.outputColorSpace=THREE.SRGBColorSpace;this.renderer.toneMapping=THREE.ACESFilmicToneMapping;this.renderer.toneMappingExposure=1.08;
    this.scene=new THREE.Scene();this.scene.background=new THREE.Color(0xb7cfca);this.scene.fog=new THREE.Fog(0xb7cfca,22,44);this.camera=new THREE.PerspectiveCamera(43,1,.1,120);this.camera.position.set(14,16,16);
    this.assets=new AssetFactory();this.input=new InputManager();this.sfx=new Sfx();this.staticRoot=new THREE.Group();this.fixtureRoot=new THREE.Group();this.itemRoot=new THREE.Group();this.customerRoot=new THREE.Group();this.playerRoot=new THREE.Group();this.buildRoot=new THREE.Group();this.scene.add(this.staticRoot,this.fixtureRoot,this.itemRoot,this.customerRoot,this.playerRoot,this.buildRoot);this.cameraRig=new CameraRig(this);
    this.players=[];this.stations=[];this.obstacles=[];this.tables=[];this.items=[];this.parties=[];this.plan=[];this.state='menu';this.levelIndex=0;this.score=0;this.combo=1;this.lives=MAX_LIVES;this.timeLeft=0;this.spawnTimer=0;this.customerSerial=0;this.partySerial=0;this.successfulTables=0;this.lastTs=performance.now();this.messageTimer=0;this.geometryPendingLife=false;this.qaEvents=[];this.selectedFixture=null;this.buildRotation=0;this.ghost=null;this.ghostPoint=new THREE.Vector3();this.buildHoverPlan=null;this.pointerNdc=new THREE.Vector2();this.raycaster=new THREE.Raycaster();this.floorPlane=new THREE.Plane(new THREE.Vector3(0,1,0),0);
    this.progress=JSON.parse(localStorage.getItem('robledo_kitchen_rush_progress')||'{"stars":[0,0,0],"tutorial":false}');
    this.business=this.loadBusiness();this.plan=this.business.plan||[];this.buildBudget=this.business.cash;
    const storedConfig=JSON.parse(localStorage.getItem('robledo_kitchen_rush_config')||'null');this.config=storedConfig||{humanCount:1,roles:['chef','prep','service'],camera:'dynamic',menu:['salad','burger']};this.sanitizeConfig();
    this.tutorialIndex=0;this.tutorialReturn='menu';this.bindUI();this.buildRecipeUI();this.buildLevelButtons();this.buildLights();this.buildStaticWorld();this.bindBuildPointer();window.__rkrGame=this;window.addEventListener('resize',()=>this.resize());this.resize();requestAnimationFrame(t=>this.loop(t));
  }
  loadBusiness(){let raw={};try{raw=JSON.parse(localStorage.getItem('robledo_kitchen_rush_business_v6')||localStorage.getItem('robledo_kitchen_rush_business_v5')||'{}');}catch(_){}const b={...BUSINESS_DEFAULT,...raw};b.cash=Math.max(0,Number(b.cash)||BUSINESS_DEFAULT.cash);b.satisfaction=clamp(Number(b.satisfaction)||BUSINESS_DEFAULT.satisfaction,0,100);b.expansionTier=clamp(Math.floor(Number(b.expansionTier)||0),0,EXPANSION_TIERS.length-1);b.plan=Array.isArray(b.plan)?b.plan:[];let refund=0;b.plan=b.plan.filter(q=>{if(LEGACY_GROCERY_KEYS.has(q.key)){refund+=Number(q.cost)||0;return false;}return true;});b.cash+=refund;return b;}
  saveBusiness(){this.business.plan=this.plan.map(q=>({...q}));localStorage.setItem('robledo_kitchen_rush_business_v6',JSON.stringify(this.business));}
  qaRecord(type,data={}){this.qaEvents.push({type,time:performance.now(),...data});if(this.qaEvents.length>300)this.qaEvents.shift();}
  tier(){return EXPANSION_TIERS[this.business.expansionTier];}
  currentBounds(){return this.tier();}
  entrancePoint(){const b=this.currentBounds();return new THREE.Vector3(b.xMax-.45,0,b.zMin+1.0);}
  exitPoint(){const b=this.currentBounds();return new THREE.Vector3(b.xMax+.75,0,b.zMin+.35);}
  availableRecipes(){return this.tier().recipes;}
  sanitizeConfig(){const allowed=this.availableRecipes(),max=this.tier().menuSlots;this.config.humanCount=clamp(Number(this.config.humanCount)||1,1,3);this.config.roles=Array.isArray(this.config.roles)&&this.config.roles.length===3?this.config.roles:['chef','prep','service'];this.config.roles=this.config.roles.map(r=>ROLE_META[r]?r:'chef');this.config.camera=['dynamic','classic','top'].includes(this.config.camera)?this.config.camera:'dynamic';const requested=Array.isArray(this.config.menu)?this.config.menu:[];this.config.menu=[...new Set(requested.filter(r=>allowed.includes(r)))].slice(0,max);for(const r of allowed)if(this.config.menu.length<Math.min(2,max)&&!this.config.menu.includes(r))this.config.menu.push(r);}
  bindUI(){
    const id=x=>document.getElementById(x);this.ui={menu:id('menu-screen'),crew:id('crew-screen'),tutorial:id('tutorial-screen'),build:id('build-ui'),recipes:id('recipe-screen'),controls:id('controls-screen'),pause:id('pause-screen'),result:id('result-screen'),geometry:id('geometry-screen'),hud:id('hud'),orders:id('orders'),playerHud:id('player-hud'),timer:id('timer'),score:id('score'),combo:id('combo'),lives:id('lives'),cash:id('cash'),satisfaction:id('satisfaction'),growth:id('growth-tier'),message:id('message'),hint:id('interaction-hint'),levelName:id('level-name')};
    id('start-btn').onclick=()=>this.openCrewSetup();id('tutorial-btn').onclick=()=>this.showTutorial('menu');id('recipes-btn').onclick=()=>this.showOverlay('recipes');id('controls-btn').onclick=()=>this.showOverlay('controls');document.querySelectorAll('.close-overlay').forEach(b=>b.onclick=()=>this.closeSoftOverlay());id('crew-back-btn').onclick=()=>this.toMenu();id('crew-continue-btn').onclick=()=>this.commitCrew();id('tutorial-next-btn').onclick=()=>this.tutorialNext();id('tutorial-prev-btn').onclick=()=>this.tutorialPrev();id('tutorial-skip-btn').onclick=()=>this.finishTutorial();id('auto-layout-btn').onclick=()=>this.autoLayout();id('clear-plan-btn').onclick=()=>this.clearPlan();id('open-restaurant-btn').onclick=()=>this.openRestaurant();id('upgrade-restaurant-btn').onclick=()=>this.upgradeRestaurant();id('resume-btn').onclick=()=>this.resume();id('restart-btn').onclick=()=>this.restartService();id('quit-btn').onclick=()=>this.toMenu();id('replay-btn').onclick=()=>this.enterBuildMode();id('result-menu-btn').onclick=()=>this.toMenu();id('next-btn').onclick=()=>{this.levelIndex=Math.min(LEVELS.length-1,this.levelIndex+1);this.openCrewSetup();};
    id('human-count').querySelectorAll('button').forEach(b=>b.onclick=()=>{this.config.humanCount=Number(b.dataset.count);this.renderCrewSetup();});id('camera-choice').querySelectorAll('button').forEach(b=>b.onclick=()=>{this.config.camera=b.dataset.camera;this.renderCrewSetup();});
    window.addEventListener('keydown',e=>{if(e.code==='Escape'){if(!this.ui.recipes.classList.contains('hidden')||!this.ui.controls.classList.contains('hidden'))this.closeSoftOverlay();else if(this.state==='playing')this.pause();else if(this.state==='paused')this.resume();else if(this.state==='build')this.toMenu();}if(e.code==='KeyM'&&this.state==='playing')this.showOverlay('recipes');if(e.code==='KeyC'&&['playing','build'].includes(this.state))this.cameraRig.cycle();if(e.code==='Home'&&['playing','build'].includes(this.state))this.cameraRig.reset();if(e.code==='KeyR'&&this.state==='build'){this.buildRotation=(this.buildRotation+Math.PI/2)%(Math.PI*2);this.updateGhost();}if(e.code==='Delete'&&this.state==='build')this.removeBuildHover();});
  }
  buildRecipeUI(){document.getElementById('recipe-grid').innerHTML=Object.values(RECIPES).map(r=>`<article class="recipe-card"><div class="recipe-icon">${r.icon}</div><h3>${r.name}</h3><div class="ingredients">${r.components.map(s=>{const[k,st]=s.split(':');return`${INGREDIENT_META[k]?.emoji||''} ${st==='raw'?'':st+' '}${INGREDIENT_META[k]?.name||k}`}).join(' + ')}</div><ul>${r.steps.map(s=>`<li>${s}</li>`).join('')}</ul></article>`).join('');}
  buildLevelButtons(){const row=document.getElementById('level-row');row.innerHTML='';LEVELS.forEach((l,i)=>{const unlocked=i===0||(this.progress.stars[i-1]||0)>0,b=document.createElement('button');b.className=`level-btn ${i===this.levelIndex?'selected':''} ${unlocked?'':'locked'}`;b.innerHTML=`${i+1}. ${l.name} ${'★'.repeat(this.progress.stars[i]||0)}`;b.disabled=!unlocked;b.onclick=()=>{this.levelIndex=i;this.buildLevelButtons();};row.appendChild(b);});}
  buildLights(){this.scene.add(new THREE.HemisphereLight(0xfff7e0,0x46666e,2.35));const sun=new THREE.DirectionalLight(0xfff0cf,4.4);sun.position.set(-10,21,12);sun.castShadow=true;sun.shadow.mapSize.set(2048,2048);sun.shadow.camera.left=-20;sun.shadow.camera.right=20;sun.shadow.camera.top=20;sun.shadow.camera.bottom=-20;this.scene.add(sun);for(const x of[-8,0,8]){const p=new THREE.PointLight(0xffdca1,18,9,2);p.position.set(x,6,-1);this.scene.add(p);}}
  buildStaticWorld(){
    while(this.staticRoot.children.length)this.staticRoot.remove(this.staticRoot.children[0]);const b=this.currentBounds(),width=b.xMax*2+1.4,depth=b.zMax-b.zMin+1.3,cz=(b.zMin+b.zMax)/2;
    const floor=mesh(rounded(width,.28,depth,.15),mat(0xcfd7d0),0,-.22,cz);floor.receiveShadow=true;this.staticRoot.add(floor);
    for(let x=Math.ceil(-b.xMax);x<=b.xMax;x+=2)for(let z=Math.ceil(b.zMin);z<=b.zMax;z+=2){const tile=mesh(rounded(1.92,.045,1.92,.04),mat(((x+z)/2)%2===0?COLORS.floorA:COLORS.floorB),x,-.045,z);tile.receiveShadow=true;tile.castShadow=false;this.staticRoot.add(tile);}
    const wallMat=mat(COLORS.wall),back=b.zMin-.68,left=-b.xMax-.68,right=b.xMax+.68;this.staticRoot.add(mesh(rounded(width,3.2,.24,.05),wallMat,0,1.6,back));this.staticRoot.add(mesh(rounded(.24,3.2,depth,.05),wallMat,left,1.6,cz));this.staticRoot.add(mesh(rounded(.24,3.2,depth*.72,.05),wallMat,right,1.6,cz+depth*.12));
    const sign=makeLabel(`ROBLEDO • ${b.name.toUpperCase()}`,'#ef7c38','#fff',640);sign.position.set(0,2.62,back+.12);sign.scale.set(Math.min(5.0,width*.31),.78,1);this.staticRoot.add(sign);
    const doorX=b.xMax-.35,door=mesh(rounded(1.65,2.45,.08,.05),mat(0x52686f),doorX,1.23,back+.12);this.staticRoot.add(door);const doorGlass=mesh(rounded(1.15,1.25,.04,.04),mat(0x9cc9d2,.18,.25),doorX,1.6,back+.2);this.staticRoot.add(doorGlass);
    if(b.id>0){const plant=this.assets.plant();plant.position.set(-b.xMax+.7,0,b.zMax-.7);plant.scale.setScalar(1.1+b.id*.08);this.staticRoot.add(plant);}this.scene.fog.near=18+b.id*3;this.scene.fog.far=36+b.id*4;
  }
  renderCrewSetup(){
    this.sanitizeConfig();document.querySelectorAll('#human-count button').forEach(b=>b.classList.toggle('active',Number(b.dataset.count)===this.config.humanCount));document.querySelectorAll('#camera-choice button').forEach(b=>b.classList.toggle('active',b.dataset.camera===this.config.camera));const slots=document.getElementById('crew-slots');slots.innerHTML='';for(let i=0;i<3;i++){const human=i<this.config.humanCount,role=this.config.roles[i],card=document.createElement('div');card.className=`crew-slot p${i+1} ${human?'human':'bot'}`;const gp=navigator.getGamepads?.()[i];card.innerHTML=`<div class="crew-head"><span class="crew-id">CREW ${i+1}</span><span class="crew-kind">${human?'HUMAN':'BOT AI'}</span></div><select data-slot="${i}">${Object.values(ROLE_META).map(r=>`<option value="${r.id}" ${r.id===role?'selected':''}>${r.icon} ${r.name}</option>`).join('')}</select><div class="role-desc">${ROLE_META[role].description}</div><div class="device-line">${human?(gp?`🎮 Gamepad ${i+1} connected`:`⌨ Keyboard layout P${i+1}`):'🤖 Autonomous task planner'}</div>`;slots.appendChild(card);}slots.querySelectorAll('select').forEach(s=>s.onchange=()=>{this.config.roles[Number(s.dataset.slot)]=s.value;this.renderCrewSetup();});
    const allowed=this.availableRecipes(),max=this.tier().menuSlots,grid=document.getElementById('menu-toggle-grid');grid.innerHTML='';for(const r of Object.values(RECIPES)){const unlocked=allowed.includes(r.id),active=this.config.menu.includes(r.id),el=document.createElement('div');el.className=`menu-toggle ${active?'active':''} ${unlocked?'':'locked'}`;el.dataset.recipe=r.id;el.innerHTML=`<span class="ico">${r.icon}</span><b>${r.name}</b><small>${unlocked?`${r.price} pts • available`:`🔒 unlock by expansion`}</small>`;if(unlocked)el.onclick=()=>this.toggleMenu(r.id);grid.appendChild(el);}document.getElementById('menu-planner-title').textContent=`CHOOSE 2–${max} DISHES • ${this.tier().name.toUpperCase()}`;document.getElementById('business-preview').innerHTML=`<b>$${Math.round(this.business.cash)}</b> cash • <b>${Math.round(this.business.satisfaction)}%</b> satisfaction • <b>${this.tier().tableCap}</b> table cap • <b>${this.tier().customerCap}</b> active parties`;document.getElementById('crew-warning').textContent=this.config.menu.length<2?`Select at least 2 available dishes.`:this.config.menu.length>max?`Maximum ${max} dishes at this restaurant size.`:'';
  }
  toggleMenu(id){if(!this.availableRecipes().includes(id))return;const has=this.config.menu.includes(id),max=this.tier().menuSlots;if(has&&this.config.menu.length>2)this.config.menu=this.config.menu.filter(x=>x!==id);else if(!has&&this.config.menu.length<max)this.config.menu.push(id);this.renderCrewSetup();}
  openCrewSetup(){this.closeAllScreens();this.state='crew';this.buildStaticWorld();this.ui.crew.classList.remove('hidden');this.renderCrewSetup();this.cameraRig.setMode(this.config.camera);}
  commitCrew(){const max=this.tier().menuSlots;if(this.config.menu.length<2||this.config.menu.length>max){document.getElementById('crew-warning').textContent=`Choose between 2 and ${max} unlocked dishes before continuing.`;return;}localStorage.setItem('robledo_kitchen_rush_config',JSON.stringify(this.config));this.showTutorial('build');}
  showTutorial(returnTo='menu'){this.closeAllScreens();this.state='tutorial';this.tutorialIndex=0;this.tutorialReturn=returnTo;this.ui.tutorial.classList.remove('hidden');this.renderTutorial();}
  renderTutorial(){const s=TUTORIAL[this.tutorialIndex],v=document.getElementById('tutorial-visual');document.getElementById('tutorial-step-label').textContent=`STEP ${this.tutorialIndex+1} / ${TUTORIAL.length}`;document.getElementById('tutorial-progress-fill').style.width=`${((this.tutorialIndex+1)/TUTORIAL.length)*100}%`;document.getElementById('tutorial-title').textContent=s.title;document.getElementById('tutorial-body').textContent=s.body;if(s.visual==='keys')v.innerHTML='<div class="keys"><kbd>WASD</kbd><kbd>E</kbd><kbd>Q</kbd><kbd>SHIFT</kbd><kbd>🎮</kbd></div>';else v.innerHTML=`<div class="big-icon">${s.visual}</div>`;document.getElementById('tutorial-prev-btn').disabled=this.tutorialIndex===0;document.getElementById('tutorial-next-btn').textContent=this.tutorialIndex===TUTORIAL.length-1?'START PLANNING':'NEXT';}
  tutorialNext(){if(this.tutorialIndex<TUTORIAL.length-1){this.tutorialIndex++;this.renderTutorial();}else this.finishTutorial();}
  tutorialPrev(){if(this.tutorialIndex>0){this.tutorialIndex--;this.renderTutorial();}}
  finishTutorial(){this.progress.tutorial=true;localStorage.setItem('robledo_kitchen_rush_progress',JSON.stringify(this.progress));if(this.tutorialReturn==='build')this.enterBuildMode();else this.toMenu();}
  fixtureCount(key){return this.plan.filter(q=>q.key===key).length;}
  fixtureCap(key){return this.tier().fixtureCaps[key]??0;}
  fixturePalette(){const keys=['table','counter','prep','stove','fryer','oven','sink','plate','trash','grocery'],grid=document.getElementById('fixture-palette');grid.innerHTML='';for(const key of keys){const f=FIXTURES[key],cap=this.fixtureCap(key),count=this.fixtureCount(key),locked=cap<=0,full=count>=cap,b=document.createElement('button');b.className=`fixture-btn ${this.selectedFixture===key?'active':''} ${locked||full?'locked':''}`;b.dataset.fixture=key;b.disabled=locked||full;b.innerHTML=`<span class="fi">${f.icon}</span><b>${f.label}</b><small>${locked?'LOCKED':`$${f.cost} • ${count}/${cap}`}</small>`;b.onclick=()=>{this.selectedFixture=key;this.fixturePalette();this.updateGhost();document.getElementById('selected-fixture-label').textContent=`Selected: ${f.label} ($${f.cost}) • ${count}/${cap}`;};grid.appendChild(b);}}
  enterBuildMode(){this.closeAllScreens();this.clearGameplay(true);this.state='build';this.buildStaticWorld();this.cameraRig.setMode(this.config.camera);this.buildBudget=this.business.cash;this.plan=(this.business.plan||[]).filter(q=>this.validPlanForTier(q));this.business.plan=this.plan;this.selectedFixture=this.fixtureCap('table')>this.fixtureCount('table')?'table':'counter';this.buildRotation=0;this.ui.build.classList.remove('hidden');this.addZoneGuides();this.renderPlanVisuals();this.fixturePalette();this.updateBuildUI();this.updateGhost();this.flash('BUILD MODE — persistent cash buys permanent restaurant assets.');}
  validPlanForTier(q){const b=this.currentBounds();return FIXTURES[q.key]&&Math.abs(q.x)<=b.xMax-.4&&q.z>=b.zMin+.4&&q.z<=b.zMax-.4;}
  renderPlanVisuals(){while(this.fixtureRoot.children.length)this.fixtureRoot.remove(this.fixtureRoot.children[0]);for(let i=0;i<this.plan.length;i++){const q=this.plan[i],v=this.createFixtureVisual(q.key,i,true);v.position.set(q.x,0,q.z);v.rotation.y=q.rot;v.userData.planId=q.id;this.fixtureRoot.add(v);}}
  addZoneGuides(){while(this.buildRoot.children.length)this.buildRoot.remove(this.buildRoot.children[0]);const b=this.currentBounds(),margin=.45,kDepth=Math.max(3.4,-b.zMin-1.0),dDepth=Math.max(3.2,b.zMax-1.0),w=b.xMax*2-margin*2;const kg=mesh(rounded(w,.025,kDepth,.08),new THREE.MeshBasicMaterial({color:COLORS.kitchenZone,transparent:true,opacity:.18,depthWrite:false}),0,.015,b.zMin+kDepth/2+.25);kg.castShadow=false;this.buildRoot.add(kg);const dg=mesh(rounded(w,.025,dDepth,.08),new THREE.MeshBasicMaterial({color:COLORS.diningZone,transparent:true,opacity:.16,depthWrite:false}),0,.016,1+dDepth/2);dg.castShadow=false;this.buildRoot.add(dg);const kl=makeLabel('KITCHEN','#3f8f97','#fff');kl.position.set(-b.xMax+1.25,.08,b.zMin+.9);kl.scale.set(1.8,.42,1);this.buildRoot.add(kl);const dl=makeLabel('DINING','#b58139','#fff');dl.position.set(-b.xMax+1.25,.08,b.zMax-.9);dl.scale.set(1.8,.42,1);this.buildRoot.add(dl);}
  bindBuildPointer(){this.canvas.addEventListener('pointermove',e=>{const r=this.canvas.getBoundingClientRect();this.pointerNdc.set((e.clientX-r.left)/r.width*2-1,-((e.clientY-r.top)/r.height)*2+1);if(this.state==='build'&&!this.cameraRig.dragging){this.raycaster.setFromCamera(this.pointerNdc,this.camera);const p=new THREE.Vector3();if(this.raycaster.ray.intersectPlane(this.floorPlane,p)){this.ghostPoint.set(snap(p.x),0,snap(p.z));this.updateGhost();this.buildHoverPlan=this.plan.reduce((best,q)=>dist2(q,this.ghostPoint)<dist2(best||{x:999,z:999},this.ghostPoint)?q:best,null);}}});this.canvas.addEventListener('pointerdown',e=>{if(this.state!=='build'||e.button!==0||this.cameraRig.dragging)return;this.placeSelectedFixture();});}
  updateGhost(){if(this.ghost){this.ghost.removeFromParent();this.ghost=null;}if(this.state!=='build'||!this.selectedFixture)return;const f=FIXTURES[this.selectedFixture];this.ghost=this.createFixtureVisual(this.selectedFixture,-1,true);this.ghost.position.set(this.ghostPoint.x,.05,this.ghostPoint.z);this.ghost.rotation.y=this.buildRotation;const ok=this.validPlacement(this.selectedFixture,this.ghostPoint.x,this.ghostPoint.z,this.buildRotation,true);this.ghost.traverse(o=>{if(o.material){o.material=o.material.clone();o.material.transparent=true;o.material.opacity=ok?.7:.28;if(!ok)o.material.color?.setHex(0xe55c51);}});this.buildRoot.add(this.ghost);}
  validPlacement(key,x,z,rot,ignoreBudget=false){const f=FIXTURES[key],b=this.currentBounds();if(this.fixtureCount(key)>=this.fixtureCap(key))return false;if(this.fixtureCap(key)<=0)return false;if(!ignoreBudget&&this.business.cash<f.cost)return false;if(Math.abs(x)>b.xMax-.65||z<b.zMin+.65||z>b.zMax-.65)return false;if(f.zone==='kitchen'&&z>-.65)return false;if(f.zone==='dining'&&z<1.05)return false;const w=Math.abs(Math.sin(rot))>.5?f.d:f.w,d=Math.abs(Math.sin(rot))>.5?f.w:f.d;for(const q of this.plan){const qf=FIXTURES[q.key],qw=Math.abs(Math.sin(q.rot))>.5?qf.d:qf.w,qd=Math.abs(Math.sin(q.rot))>.5?qf.w:qf.d;if(Math.abs(x-q.x)<(w+qw)*.52&&Math.abs(z-q.z)<(d+qd)*.52)return false;}return true;}
  placeSelectedFixture(){const key=this.selectedFixture;if(!key)return;const f=FIXTURES[key],x=this.ghostPoint.x,z=this.ghostPoint.z;if(!this.validPlacement(key,x,z,this.buildRotation)){this.sfx.bad();document.getElementById('build-status').textContent='Cannot place here: check unlocked capacity, zone, overlap or business cash.';return;}const q={id:crypto.randomUUID?.()||`${Date.now()}-${Math.random()}`,key,x,z,rot:this.buildRotation,cost:f.cost};this.plan.push(q);this.business.cash-=f.cost;this.buildBudget=this.business.cash;const v=this.createFixtureVisual(key,this.plan.length-1,true);v.position.set(x,0,z);v.rotation.y=q.rot;v.userData.planId=q.id;this.fixtureRoot.add(v);this.sfx.build();this.saveBusiness();this.fixturePalette();this.updateBuildUI();this.updateGhost();}
  removeBuildHover(){if(!this.buildHoverPlan||dist2(this.buildHoverPlan,this.ghostPoint)>1.7)return;const q=this.buildHoverPlan;this.plan=this.plan.filter(p=>p.id!==q.id);this.business.cash+=q.cost;this.buildBudget=this.business.cash;const v=this.fixtureRoot.children.find(c=>c.userData.planId===q.id);v?.removeFromParent();this.buildHoverPlan=null;this.saveBusiness();this.fixturePalette();this.updateBuildUI();this.updateGhost();}
  clearPlan(){for(const q of this.plan)this.business.cash+=q.cost;this.plan=[];this.business.plan=[];this.buildBudget=this.business.cash;while(this.fixtureRoot.children.length)this.fixtureRoot.remove(this.fixtureRoot.children[0]);this.saveBusiness();this.fixturePalette();this.updateBuildUI();this.updateGhost();}
  autoLayout(){this.clearPlan();const b=this.currentBounds(),add=(key,x,z,rot=0)=>{const f=FIXTURES[key];if(this.business.cash<f.cost||this.fixtureCount(key)>=this.fixtureCap(key))return false;if(!this.validPlacement(key,x,z,rot))return false;this.plan.push({id:`auto-${this.plan.length}-${Date.now()}`,key,x,z,rot,cost:f.cost});this.business.cash-=f.cost;return true;};const backZ=b.zMin+1.2;add('plate',-b.xMax+1.15,backZ);add('sink',-b.xMax+2.85,backZ);add('trash',-b.xMax+4.55,backZ);add('grocery',b.xMax-3.15,backZ);const workZ=Math.min(-1.55,b.zMin+3.6),workXs=[-b.xMax+1.4,-b.xMax+3.2,-b.xMax+5.0,-b.xMax+6.8].filter(x=>x<b.xMax-1);let k=0;if(this.needsStation('prep'))add('prep',workXs[k++]??-3,workZ);if(this.needsStation('stove'))add('stove',workXs[k++]??-1,workZ);if(this.needsStation('fryer'))add('fryer',workXs[k++]??1,workZ);if(this.needsStation('oven'))add('oven',workXs[k++]??3,workZ);add('counter',Math.min(b.xMax-1.25,2.8),workZ);const tableXs=this.tier().minTables===2?[-2.2,2.2]:[-4,0,4];for(let i=0;i<this.tier().minTables;i++)add('table',clamp(tableXs[i],-b.xMax+1.5,b.xMax-1.5),Math.min(b.zMax-1.5,2.8));this.business.plan=this.plan;this.buildBudget=this.business.cash;this.saveBusiness();this.renderPlanVisuals();this.fixturePalette();this.updateBuildUI();this.updateGhost();document.getElementById('build-status').textContent='Compact recommended layout installed: one grocery market wall + real production stations.';}
  requiredIngredients(){const set=new Set();for(const id of this.config.menu)for(const sig of RECIPES[id].components)set.add(sig.split(':')[0]);return[...set];}
  needsStation(type){for(const id of this.config.menu){const r=RECIPES[id];if(type==='oven'&&r.baked)return true;for(const sig of r.components){const[,state]=sig.split(':');if(type==='prep'&&state==='chopped')return true;if(type==='stove'&&state==='cooked')return true;if(type==='fryer'&&state==='fried')return true;}}return false;}
  planRequirements(){const min=this.tier().minTables,req=[{label:`At least ${min} dining tables`,ok:this.fixtureCount('table')>=min},{label:'Plate rack',ok:this.plan.some(q=>q.key==='plate')},{label:'Sink',ok:this.plan.some(q=>q.key==='sink')},{label:'Trash',ok:this.plan.some(q=>q.key==='trash')},{label:'Grocery Market + Freezer',ok:this.plan.some(q=>q.key==='grocery')},{label:'Assembly counter',ok:this.plan.some(q=>q.key==='counter')}];for(const st of['prep','stove','fryer','oven'])if(this.needsStation(st))req.push({label:FIXTURES[st].label,ok:this.plan.some(q=>q.key===st)});return req;}
  updateBuildUI(){const t=this.tier(),next=EXPANSION_TIERS[t.id+1];document.getElementById('build-budget').textContent=`$${Math.round(this.business.cash)}`;document.getElementById('business-tier-name').textContent=`TIER ${t.id+1} • ${t.name}`;document.getElementById('business-satisfaction').textContent=`${Math.round(this.business.satisfaction)}%`;document.getElementById('capacity-summary').textContent=`${this.fixtureCount('table')}/${t.tableCap} tables • ${t.customerCap} active parties • ${t.menuSlots} menu slots`;const up=document.getElementById('upgrade-restaurant-btn');if(next){up.textContent=`EXPAND → ${next.name.toUpperCase()} • $${t.upgradeCost}`;up.disabled=this.business.cash<t.upgradeCost||this.business.satisfaction<t.requiredSatisfaction;document.getElementById('upgrade-requirement').textContent=`Requires $${t.upgradeCost} cash + ${t.requiredSatisfaction}% satisfaction`; }else{up.textContent='MAXIMUM RESTAURANT SIZE';up.disabled=true;document.getElementById('upgrade-requirement').textContent='All expansion tiers unlocked.';}const req=this.planRequirements();document.getElementById('build-requirements').innerHTML=req.map(r=>`<div class="req ${r.ok?'ok':'bad'}"><span>${r.ok?'✓':'○'} ${r.label}</span><b>${r.ok?'READY':'NEEDED'}</b></div>`).join('');document.getElementById('open-restaurant-btn').disabled=req.some(r=>!r.ok);}
  upgradeRestaurant(){const t=this.tier(),next=EXPANSION_TIERS[t.id+1];if(!next)return;if(this.business.cash<t.upgradeCost||this.business.satisfaction<t.requiredSatisfaction){this.sfx.bad();this.flash(`Expansion needs $${t.upgradeCost} and ${t.requiredSatisfaction}% satisfaction.`);return;}this.business.cash-=t.upgradeCost;this.business.expansionTier++;this.buildBudget=this.business.cash;this.sanitizeConfig();this.saveBusiness();this.buildStaticWorld();this.addZoneGuides();this.fixturePalette();this.updateBuildUI();this.cameraRig.reset();this.sfx.serve();this.flash(`${next.name} unlocked! More space, tables, customers, stations and recipes are available.`);}
  createFixtureVisual(key,index=0,build=false){const f=FIXTURES[key];let g;if(key==='grocery')g=this.assets.grocery(this.requiredIngredients());else if(key==='table')g=this.assets.table(Math.max(0,index));else if(key==='counter')g=this.assets.counter();else g=this.assets.station(key,key==='sink'?0x4fa8b8:key==='stove'||key==='oven'?0xd5644d:COLORS.orange);if(build&&key!=='table'){const l=makeLabel(f.label.toUpperCase(),f.zone==='kitchen'?'#447f86':'#a67838','#fff',480);l.position.set(0,1.72,0);l.scale.set(1.85,.34,1);g.add(l);}return g;}
  clearGameplay(keepStatic=false){for(const r of[this.fixtureRoot,this.itemRoot,this.customerRoot,this.playerRoot])while(r.children.length)r.remove(r.children[0]);this.stations=[];this.obstacles=[];this.tables=[];this.items=[];this.parties=[];if(!keepStatic)while(this.buildRoot.children.length)this.buildRoot.remove(this.buildRoot.children[0]);}
  buildLiveKitchen(){while(this.buildRoot.children.length)this.buildRoot.remove(this.buildRoot.children[0]);while(this.fixtureRoot.children.length)this.fixtureRoot.remove(this.fixtureRoot.children[0]);this.stations=[];this.tables=[];this.obstacles=[];let tableIndex=0;for(const q of this.plan){const f=FIXTURES[q.key],group=this.createFixtureVisual(q.key,q.key==='table'?tableIndex:0,false);group.position.set(q.x,0,q.z);group.rotation.y=q.rot;this.fixtureRoot.add(group);if(q.key==='table'){const t={id:tableIndex++,type:'table',pos:new THREE.Vector3(q.x,0,q.z),party:null,dirty:0,cleanClaim:null,group,seats:[]};for(const a of[0,Math.PI/2,Math.PI,Math.PI*1.5]){const local=new THREE.Vector3(Math.sin(a)*1.25,0,Math.cos(a)*1.25).applyAxisAngle(new THREE.Vector3(0,1,0),q.rot);t.seats.push(new THREE.Vector3(q.x+local.x,0,q.z+local.z));}this.tables.push(t);}else if(q.key==='grocery'){const kinds=this.requiredIngredients(),axis=new THREE.Vector3(0,1,0),n=Math.max(1,kinds.length);kinds.forEach((kind,i)=>{const lx=n===1?0:-2.05+i*(4.1/(n-1)),local=new THREE.Vector3(lx,0,1.18).applyAxisAngle(axis,q.rot),pos=new THREE.Vector3(q.x+local.x,0,q.z+local.z);this.stations.push({type:'storage',kind,pos,x:pos.x,z:pos.z,group,slot:null,reservedBy:null,storageIndex:i});});}else{const s={type:q.key,kind:null,pos:new THREE.Vector3(q.x,0,q.z),x:q.x,z:q.z,group,slot:null,progress:0,cook:0,burn:0,ready:false,bar:null,reservedBy:null};if(['prep','stove','fryer','oven','sink'].includes(s.type)){const bg=mesh(rounded(.9,.075,.055,.02),mat(0x243139),0,1.45,.57);bg.castShadow=false;const fill=mesh(rounded(.84,.055,.06,.015),mat(s.type==='sink'?0x4fb7dc:s.type==='prep'?0xe4aa54:0x69b36f),-.42,1.45,.605);fill.geometry.translate(.42,0,0);fill.scale.x=.001;fill.castShadow=false;group.add(bg,fill);s.bar=fill;}this.stations.push(s);}const w=Math.abs(Math.sin(q.rot))>.5?f.d:f.w,d=Math.abs(Math.sin(q.rot))>.5?f.w:f.d;this.obstacles.push({x:q.x,z:q.z,w:w*(q.key==='table'?.68:.95),d:d*(q.key==='table'?.68:.95),type:q.key});}}
  spawnPlayers(){const b=this.currentBounds(),z=clamp(.1,b.zMin+2,b.zMax-2),spread=Math.min(2.1,b.xMax/3),starts=[new THREE.Vector3(-spread,0,z),new THREE.Vector3(0,0,z),new THREE.Vector3(spread,0,z)],colors=[COLORS.red,COLORS.blue,COLORS.yellow];this.players=[];for(let i=0;i<3;i++)this.players.push(new CrewMember(this,i,colors[i],starts[i],this.config.roles[i],i<this.config.humanCount));this.renderPlayerHud();}
  renderPlayerHud(){this.ui.playerHud.innerHTML=this.players.map((p,i)=>`<div class="player-chip p${i+1} ${p.human?'human':'bot'}"><div class="line"><b>P${i+1} • ${ROLE_META[p.role].icon} ${ROLE_META[p.role].name}</b><em>${p.human?'HUMAN':'BOT'}</em></div><span id="p${i+1}-held">${p.human?'empty hands':'AI ready'}</span></div>`).join('');}
  openRestaurant(){const missing=this.planRequirements().filter(r=>!r.ok);if(missing.length){this.sfx.bad();document.getElementById('build-status').textContent=`Missing: ${missing.map(x=>x.label).join(', ')}`;return;}this.business.plan=this.plan;this.saveBusiness();this.closeAllScreens();this.clearGameplay();this.buildLiveKitchen();this.state='playing';const L=LEVELS[this.levelIndex];this.score=0;this.combo=1;this.lives=MAX_LIVES;this.timeLeft=L.duration;this.spawnTimer=4;this.customerSerial=0;this.partySerial=0;this.successfulTables=0;this.geometryPendingLife=false;this.cameraRig.setMode(this.config.camera);this.scene.background.setHex(L.tint);this.scene.fog.color.setHex(L.tint);this.spawnPlayers();this.ui.hud.classList.remove('hidden');this.ui.orders.classList.remove('hidden');this.ui.playerHud.classList.remove('hidden');this.ui.levelName.textContent=`${L.name} • ${this.tier().name}`;this.updateUI();this.flash(`OPEN — customers browse, crew takes orders, then FAST LIFE ≤ ${FAST_SERVICE_SECONDS}s`);}
  restartService(){this.openRestaurant();}
  moveCrew(p,move){const b=this.currentBounds(),tryPos=p.group.position.clone();tryPos.x+=move.x;if(!this.blocked(p,tryPos))p.group.position.x=tryPos.x;tryPos.copy(p.group.position);tryPos.z+=move.z;if(!this.blocked(p,tryPos))p.group.position.z=tryPos.z;p.group.position.x=clamp(p.group.position.x,-b.xMax+.5,b.xMax-.5);p.group.position.z=clamp(p.group.position.z,b.zMin+.5,b.zMax-.5);}
  blocked(p,pos){for(const o of this.obstacles)if(Math.abs(pos.x-o.x)<o.w/2+p.radius&&Math.abs(pos.z-o.z)<o.d/2+p.radius)return true;for(const other of this.players)if(other!==p&&dist2(pos,other.group.position)<p.radius+other.radius-.04)return true;return false;}
  nearestInteractable(pos,range){let best=null,bd=range;for(const s of this.stations){const d=dist2(pos,s.pos);if(d<bd){best=s;bd=d;}}for(const t of this.tables){const d=dist2(pos,t.pos);if(d<bd){best=t;bd=d;}}for(const i of this.items){if(i.dead||i.airborne||i.onSurface)continue;const d=dist2(pos,i.group.position);if(d<bd){best=i;bd=d;}}return best;}
  spawnItem(kind,state='raw',plate=false,pos=null){const i=new WorldItem(this,kind,state,plate);this.items.push(i);if(pos)i.group.position.copy(pos);return i;}
  interact(p,target,dt,held){if(target instanceof WorldItem){if(!p.held)p.pick(target);return;}if(target.type==='table'){this.interactTable(p,target);return;}if(target.type==='storage'){if(!p.held){const i=this.spawnItem(target.kind,'raw',false,target.pos.clone().add(new THREE.Vector3(0,1.1,0)));p.pick(i);this.qaRecord('storage-pick',{by:p.human?`P${p.index+1}`:`BOT ${p.index+1}`,ingredient:target.kind});}return;}if(target.type==='plate'){if(p.held?.isPlate&&!p.held.dirty){p.held.dispose();p.held=null;this.score+=5;this.qaRecord('plate-return',{by:p.human?`P${p.index+1}`:`BOT ${p.index+1}`});return;}if(!p.held){const i=this.spawnItem('plate','raw',true,target.pos.clone().add(new THREE.Vector3(0,1.1,0)));p.pick(i);}return;}if(target.type==='trash'){if(p.held){p.held.dispose();p.held=null;this.sfx.bad();}return;}if(target.type==='counter'){this.interactCounter(p,target);return;}if(target.type==='prep'){this.interactPrep(p,target,dt);return;}if(['stove','fryer','oven'].includes(target.type)){this.interactCookStation(p,target);return;}if(target.type==='sink'){this.interactSink(p,target,dt);return;}}
  interactCounter(p,s){if(!s.slot&&p.held){s.slot=p.held;p.held=null;s.slot.onSurface=true;s.slot.group.position.copy(s.pos).add(new THREE.Vector3(0,1.2,0));return;}if(s.slot&&!p.held){p.pick(s.slot);s.slot.onSurface=false;s.slot=null;return;}if(s.slot&&p.held){let plate=null,ing=null;if(s.slot.isPlate&&!p.held.isPlate){plate=s.slot;ing=p.held;}else if(p.held.isPlate&&!s.slot.isPlate){plate=p.held;ing=s.slot;}if(plate&&ing&&!plate.dirty){plate.components.push({kind:ing.kind,state:ing.state});ing.dispose();if(ing===p.held)p.held=null;else s.slot=plate===p.held?null:plate;if(plate===p.held)s.slot=null;this.sfx.pickup();}}}
  interactPrep(p,s,dt){if(!s.slot&&p.held&&!p.held.isPlate&&INGREDIENT_META[p.held.kind]?.choppable&&p.held.state==='raw'){s.slot=p.held;p.held=null;s.slot.onSurface=true;s.slot.group.position.copy(s.pos).add(new THREE.Vector3(0,1.22,0));s.progress=0;return;}if(s.slot&&s.slot.state==='raw'){s.progress+=dt*p.workMultiplier('prep')*1.05;if(s.bar)s.bar.scale.x=clamp(s.progress/1.7,.001,1);if(s.progress>=1.7){s.slot.setState('chopped');s.progress=0;if(s.bar)s.bar.scale.x=.001;this.sfx.chop();}}else if(s.slot&&!p.held){p.pick(s.slot);s.slot.onSurface=false;s.slot=null;}}
  interactCookStation(p,s){if(!s.slot&&p.held){if(s.type==='stove'&&!p.held.isPlate&&p.held.kind==='meat'&&p.held.state==='raw'){s.slot=p.held;p.held=null;}else if(s.type==='fryer'&&!p.held.isPlate&&p.held.kind==='potato'&&p.held.state==='chopped'){s.slot=p.held;p.held=null;}else if(s.type==='oven'&&p.held.isPlate&&!p.held.dirty&&!p.held.baked){const rawMatch=Object.values(RECIPES).some(r=>r.baked&&[...r.components].sort().join('|')===p.held.components.map(signature).sort().join('|'));if(!rawMatch)return;s.slot=p.held;p.held=null;}else return;s.slot.onSurface=true;s.slot.group.position.copy(s.pos).add(new THREE.Vector3(0,1.25,0));s.cook=0;s.ready=false;return;}if(s.slot&&!p.held){p.pick(s.slot);s.slot.onSurface=false;s.slot=null;s.cook=0;s.ready=false;if(s.bar)s.bar.scale.x=.001;}}
  interactSink(p,s,dt){if(!s.slot&&p.held?.isPlate&&p.held.dirty){s.slot=p.held;p.held=null;s.slot.onSurface=true;s.slot.group.position.copy(s.pos).add(new THREE.Vector3(0,1.22,0));s.progress=0;return;}if(s.slot?.dirty){s.progress+=dt*p.workMultiplier('sink');if(s.bar)s.bar.scale.x=clamp(s.progress/1.8,.001,1);if(s.progress>=1.8){s.slot.setDirty(false);s.slot.components=[];s.slot.baked=false;s.slot.burnt=false;s.progress=0;if(s.bar)s.bar.scale.x=.001;this.score+=15;this.sfx.serve();}}else if(s.slot&&!p.held){p.pick(s.slot);s.slot.onSurface=false;s.slot=null;}}
  interactTable(p,t){if(t.party?.state==='readyToOrder'&&!p.held){t.party.confirmOrder(p);return;}if(t.party?.state==='waiting'&&p.held?.isPlate){const rid=identifyRecipe(p.held);if(rid&&t.party.serve(rid,p)){p.held.dispose();p.held=null;}else{this.sfx.bad();this.flash(`Table ${t.id+1} did not order that dish.`);}return;}if(!t.party&&t.dirty>0&&!p.held){const plate=this.spawnItem('plate','raw',true,t.pos.clone().add(new THREE.Vector3(0,1.1,0)));plate.setDirty(true);p.pick(plate);t.dirty--;this.flash(`Picked up dirty plate from Table ${t.id+1}`);}}
  updateStations(dt){for(const s of this.stations){if(!['stove','fryer','oven'].includes(s.type)||!s.slot)continue;s.cook+=dt;const roleGrace=this.players.some(p=>p.role==='chef')?1.15:1,readyAt=s.type==='oven'?4.1:3.5,burnAt=(s.type==='oven'?8.3:7.1)*roleGrace;if(!s.ready&&s.cook>=readyAt){s.ready=true;if(s.type==='stove')s.slot.setState('cooked');if(s.type==='fryer')s.slot.setState('fried');if(s.type==='oven')s.slot.baked=true;this.sfx.cook();}if(s.cook>=burnAt&&!s.slot.burnt){s.slot.burnt=true;if(!s.slot.isPlate)s.slot.setState('burnt');this.sfx.bad();this.flash(`${s.type.toUpperCase()} item burned!`);}if(s.bar)s.bar.scale.x=s.ready?1:clamp(s.cook/readyAt,.001,1);}}
  updateThrown(dt){for(const i of this.items){if(i.dead||!i.airborne)continue;i.velocity.y-=9.8*dt;i.group.position.addScaledVector(i.velocity,dt);for(const p of this.players){if(!p.human||p.held)continue;if(i.group.position.y<1.7&&i.group.position.y>.55&&dist2(i.group.position,p.group.position)<.58){i.airborne=false;i.velocity.set(0,0,0);p.pick(i);this.flash(`P${p.index+1} caught ${i.description()}!`);break;}}if(i.airborne&&i.group.position.y<=.22){i.group.position.y=.22;i.airborne=false;i.velocity.set(0,0,0);}}this.items=this.items.filter(i=>!i.dead);}
  spawnParty(){const t=this.tier(),active=this.parties.filter(p=>!['gone','leaving'].includes(p.state));if(active.length>=t.customerCap)return;const free=this.tables.filter(tb=>!tb.party&&tb.dirty===0);if(!free.length)return;const table=free[Math.floor(Math.random()*free.length)],size=1+Math.floor(Math.random()*Math.min(t.partySize,table.seats.length)),type=CUSTOMER_TYPES[Math.floor(Math.random()*CUSTOMER_TYPES.length)],p=new CustomerParty(this,table,size,this.config.menu,LEVELS[this.levelIndex].patience,type);table.party=p;this.parties.push(p);}
  completeTable(party,server,tip){const fast=party.waitElapsed<=FAST_SERVICE_SECONDS,urgency=clamp(party.patience/party.patienceMax,0,1),baseRevenue=party.originalRecipes.reduce((sum,id)=>sum+RECIPES[id].price,0),revenue=Math.round(baseRevenue*.12+tip*.14+(fast?18:0));this.business.cash+=revenue;this.business.totalRevenue+=revenue;this.business.totalServed++;const satisfactionGain=fast?5:urgency>.5?3:1;this.business.satisfaction=clamp(this.business.satisfaction+satisfactionGain,0,100);let lifeText='';if(fast){this.business.fastServes++;if(this.lives<MAX_LIVES){this.lives++;lifeText=' • +1 LIFE';}else lifeText=' • LIFE BANK FULL';}this.saveBusiness();this.flash(`Table ${party.table.id+1} complete in ${Math.ceil(party.waitElapsed)}s • +$${revenue} • +${satisfactionGain}% SAT${lifeText}`);}
  walkout(p){if(!['readyToOrder','waiting'].includes(p.state))return;p.forceLeave();this.combo=1;this.lives=Math.max(0,this.lives-1);this.business.satisfaction=clamp(this.business.satisfaction-10,0,100);this.business.cash=Math.max(0,this.business.cash-12);this.saveBusiness();this.sfx.bad();this.flash(`Table ${p.table.id+1} walked out! -1 life • -10% satisfaction • -$12`);if(this.lives>0)this.openGeometryRescue();else this.endLevel(true);}
  openGeometryRescue(){this.state='geometry';this.geometryPendingLife=true;const q=GEOMETRY_QUESTIONS[Math.floor(Math.random()*GEOMETRY_QUESTIONS.length)];document.getElementById('geometry-question').textContent=q.q;const box=document.getElementById('geometry-options');box.innerHTML='';q.a.forEach((a,i)=>{const b=document.createElement('button');b.textContent=a;b.onclick=()=>{if(i===q.c){b.classList.add('correct');this.lives=Math.min(MAX_LIVES,this.lives+1);this.flash('Correct! Team life restored.');this.sfx.serve();}else{b.classList.add('wrong');this.sfx.bad();this.flash('Incorrect. Continue with the remaining lives.');}setTimeout(()=>{this.ui.geometry.classList.add('hidden');this.state='playing';this.geometryPendingLife=false;},550);};box.appendChild(b);});this.ui.geometry.classList.remove('hidden');}
  updateOrdersUI(){const active=this.parties.filter(p=>['readyToOrder','waiting'].includes(p.state));this.ui.orders.innerHTML=active.slice(0,this.tier().customerCap).map(p=>{const pct=clamp(p.patience/p.patienceMax*100,0,100);if(p.state==='readyToOrder')return`<article class="order-card needs-waiter ${pct<28?'urgent':''}"><div class="top"><span>TABLE ${p.table.id+1}</span><span>🙋 ${Math.ceil(p.orderWaitElapsed)}s</span></div><div class="customer-type">${p.type.label} • WAITER NEEDED</div><div class="dishes">📝 TAKE ORDER</div><div class="bar"><div class="fill" style="width:${pct}%"></div></div></article>`;const fastLeft=Math.max(0,FAST_SERVICE_SECONDS-p.waitElapsed),fast=p.waitElapsed<=FAST_SERVICE_SECONDS;return`<article class="order-card ${pct<28?'urgent':''} ${fast?'fast':''}"><div class="top"><span>TABLE ${p.table.id+1}</span><span>${Math.ceil(p.patience)}s</span></div><div class="customer-type">${p.type.label} • ${fast?`❤️ ${Math.ceil(fastLeft)}s`:'FAST BONUS MISSED'} • ${p.orderTakenBy||'CREW'}</div><div class="dishes">${p.orders.map(o=>RECIPES[o.recipeId].icon+(o.claimedBy?'·':'')).join(' ')}</div><div class="bar"><div class="fill" style="width:${pct}%"></div></div></article>`;}).join('');}
  updateUI(){this.ui.timer.textContent=fmtTime(this.timeLeft);this.ui.score.textContent=Math.round(this.score);this.ui.combo.textContent=`x${this.combo}`;this.ui.lives.textContent='❤'.repeat(this.lives)+'♡'.repeat(MAX_LIVES-this.lives);this.ui.cash.textContent=`$${Math.round(this.business.cash)}`;this.ui.satisfaction.textContent=`${Math.round(this.business.satisfaction)}%`;this.ui.growth.textContent=`T${this.business.expansionTier+1}`;for(const p of this.players){const el=document.getElementById(`p${p.index+1}-held`);if(el)el.textContent=p.human?(p.held?p.held.description():'empty hands'):(p.bot.task?`AI: ${p.bot.task.kind==='dish'?`cooking ${RECIPES[p.bot.task.recipeId]?.name}`:p.bot.task.kind==='order'?'taking order':'cleaning'}`:'AI ready');}this.updateOrdersUI();}
  flash(text){this.ui.message.textContent=text;this.ui.message.classList.remove('hidden');this.messageTimer=2.8;}
  showOverlay(name){if(this.state==='playing')this.state='softpause';this.ui[name].classList.remove('hidden');}
  closeSoftOverlay(){this.ui.recipes.classList.add('hidden');this.ui.controls.classList.add('hidden');if(this.state==='softpause')this.state='playing';}
  closeAllScreens(){for(const k of['menu','crew','tutorial','recipes','controls','pause','result','geometry'])this.ui[k].classList.add('hidden');this.ui.build.classList.add('hidden');this.ui.hud.classList.add('hidden');this.ui.orders.classList.add('hidden');this.ui.playerHud.classList.add('hidden');this.ui.hint.classList.add('hidden');}
  pause(){this.state='paused';this.ui.pause.classList.remove('hidden');}
  resume(){this.ui.pause.classList.add('hidden');this.state='playing';}
  toMenu(){this.saveBusiness();this.closeAllScreens();this.clearGameplay();this.state='menu';this.ui.menu.classList.remove('hidden');this.scene.background.setHex(0xb7cfca);this.scene.fog.color.setHex(0xb7cfca);this.buildStaticWorld();this.cameraRig.setMode('classic');this.buildLevelButtons();}
  endLevel(failed=false){if(this.state==='result')return;this.state='result';const L=LEVELS[this.levelIndex],stars=failed?0:this.score>=L.thresholds[2]?3:this.score>=L.thresholds[1]?2:this.score>=L.thresholds[0]?1:0,bonus=failed?0:stars*35;this.business.cash+=bonus;this.business.satisfaction=clamp(this.business.satisfaction+(stars>=2?2:0),0,100);this.saveBusiness();this.progress.stars[this.levelIndex]=Math.max(this.progress.stars[this.levelIndex]||0,stars);localStorage.setItem('robledo_kitchen_rush_progress',JSON.stringify(this.progress));document.getElementById('result-title').textContent=failed?'KITCHEN CLOSED EARLY':'SERVICE COMPLETE';document.getElementById('result-stars').textContent='★'.repeat(stars)+'☆'.repeat(3-stars);document.getElementById('result-stats').innerHTML=`<p><b>${Math.round(this.score)}</b> points • <b>$${Math.round(this.business.cash)}</b> business cash</p><p>${this.successfulTables} tables • ${Math.round(this.business.satisfaction)}% satisfaction • ${this.business.fastServes} career fast-service bonuses</p><p>${bonus?`Shift investment bonus: +$${bonus}`:'No shift bonus'} • Current restaurant: ${this.tier().name}</p>`;this.ui.result.classList.remove('hidden');this.buildLevelButtons();}
  resize(){const w=innerWidth,h=innerHeight;this.renderer.setSize(w,h,false);this.camera.aspect=w/h;this.camera.updateProjectionMatrix();}
  updateBuildHoverHint(){if(this.state!=='build')return;const q=this.buildHoverPlan;if(q&&dist2(q,this.ghostPoint)<1.7)document.getElementById('build-status').textContent=`Hovering ${FIXTURES[q.key].label}. Delete refunds $${q.cost}.`;}
  loop(ts){const dt=Math.min(.04,Math.max(.001,(ts-this.lastTs)/1000));this.lastTs=ts;if(this.state==='playing'){for(const p of this.players)p.update(dt);this.updateStations(dt);this.updateThrown(dt);for(const p of this.parties)p.update(dt);this.parties=this.parties.filter(p=>p.state!=='gone');this.spawnTimer-=dt;if(this.spawnTimer<=0){this.spawnParty();const tierFactor=1+this.business.expansionTier*.05;this.spawnTimer=LEVELS[this.levelIndex].spawnEvery/tierFactor*(.9+Math.random()*.32);}this.timeLeft-=dt;if(this.timeLeft<=0)this.endLevel(false);this.updateUI();const p1=this.players.find(p=>p.human);if(p1){const n=this.nearestInteractable(p1.group.position,1.55);if(n){this.ui.hint.textContent=`P${p1.index+1} • ${n.type==='table'?`TABLE ${n.id+1}${n.party?.state==='readyToOrder'?' • TAKE ORDER':''}`:n.type==='storage'?`${INGREDIENT_META[n.kind]?.name||n.kind} • GROCERY MARKET`:n instanceof WorldItem?n.description().toUpperCase():(FIXTURES[n.type]?.label||n.type).toUpperCase()} • INTERACT`;this.ui.hint.classList.remove('hidden');}else this.ui.hint.classList.add('hidden');}}
    if(this.state==='build')this.updateBuildHoverHint();if(this.messageTimer>0){this.messageTimer-=dt;if(this.messageTimer<=0)this.ui.message.classList.add('hidden');}this.cameraRig.update(dt);this.renderer.render(this.scene,this.camera);requestAnimationFrame(t=>this.loop(t));}
}

new Game();
